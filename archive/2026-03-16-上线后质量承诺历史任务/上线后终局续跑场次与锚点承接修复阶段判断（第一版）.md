# 上线后终局续跑场次与锚点承接修复阶段判断（第一版）

## 这次只回答一件事
- 按当前已经有的真实结果看，终局续跑里“场次挂死”和“锚点漂移误报”现在到底推进到了哪一步。

## 先说结论

当前正式结论是：

**终局续跑的场次承接与锚点后验判断，已经解决本质问题，现已正式到 `阶段通过`。**

更直白地说：

**现在续跑不会再一直挂在 `第 10 场` 原地打转，账本也不会再把正常窗口滚动误报成 `entity_anchor_shift`。**

## 为什么现在可以这样判断

因为当前已经连续确认 5 件事：

### 第一
- 根因已经钉死，而且没有走偏。
- `第 10 场挂死` 的根因，不是人物不会写，也不是提示词不够狠。
- 根因是续跑计划把“下一场继续往下写”，错绑成了“总集数上限里继续找位子”。

### 第二
- `entity_anchor_shift` 的根因也已经钉死。
- 不是每次都真的发生了实体承接漂移。
- 而是后验判断一边拿“最近 5 个开放钩子”的窗口整体做比较，一边开放钩子 id 又跟窗口位置绑定，导致窗口自然往前滚时也会被误判。

### 第三
- 根因修改已经真正落下去。
- 续跑计划现在不再把续跑场次卡死在 `targetEpisodes=10` 里。
- 开放钩子 id 现在改成稳定 id，不再跟窗口里的第几个绑死。
- 后验判断也只对“同一条钩子自己的锚点有没有乱掉”做比较，不再把正常滚动当成漂移。

### 第四
- 第一笔真实验证样本 `tools/e2e/out/userdata-real-verify-mms94uhy/workspace/projects.json` 已经证明场次承接回正。
- 本轮耗时约 `47.9 秒`。
- 软件已关闭，没有残留。
- 结果不再继续写 `第 10 场`，而是直接写出 `第 14 场`。
- 这说明“场次挂死”这条本质问题已经被真正打中。

### 第五
- 第二笔真实验证样本 `tools/e2e/out/userdata-real-verify-mms9881s/workspace/projects.json` 已经同时证明两件事：
- 本轮耗时约 `51.6 秒`
- 软件已关闭，没有残留
- 场次继续从 `第 14 场` 往后写到 `第 15 场`
- `postflight.issues` 已经清空，`entity_anchor_shift` 不再误报
- 账本正式给出“断言通过”

## 这两笔结果共同证明了什么

当前共同证明了 3 件事：

### 一、续跑真正接上了
- 现在不是“内容还在走，但编号死着不动”。
- 是编号和内容都一起往后走了。

### 二、后验判断重新可信了
- 现在不是“每次窗口滚一格就报漂移”。
- 只有真有问题，后验判断才应该响。

### 三、这轮修的是根因，不是补丁
- 没有靠 gate
- 没有靠命中词
- 没有靠 recovery 链事后改编号
- 也没有靠正文抽名补名单

## 现在还不能说什么

当前还不能直接说：

### 1. 不能说剧本终局层已经全部收死
- 这次收住的是“续跑场次承接”和“锚点漂移误报”。
- 不等于终局创作本身所有问题都已经一起完成。

### 2. 不能说历史旧样本会自动变干净
- 旧样本里已经写坏的 `第 10 场` 重复历史还在。
- 但新的续跑样本已经证明：从现在往后跑，链路已经回正。

## 这次阶段判断真正解决了什么本质问题

这次解决的不是“又多跑出来两场”。

而是：

**现在终于可以正式确认：终局续跑链路已经不再把“继续往下写”卡死在 `第 10 场`，账本也不再把正常窗口滚动误判成 `entity_anchor_shift`。**

## 最后结论

这张修复任务现在可以正式定性为：

- 已完成终局续跑场次承接修复
- 已完成锚点漂移误报修复
- 第 1 笔真实续跑耗时约 `47.9 秒`
- 第 2 笔真实续跑耗时约 `51.6 秒`
- 两轮测试后软件都已关闭，没有残留
- 当前任务现已正式到 `阶段通过`
