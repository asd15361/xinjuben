# 聊天记录

这个文件只保留一条当前主线。

断线恢复时，先看 `AGENTS.md`，再看这份文件。

## 当前主线

- 当前新增进展（2026-04-18 前后端分离终极收口行动·阶段 1）：
  - 已确认旧主线 `2026-04-14-主线.md` 完成并准备归档，不再继续冒充当前主线
  - 当前工程主权已切换到：
    - `server/` = 唯一正式业务入口
    - PocketBase = 唯一项目真相存储
    - `src/main` / `src/preload` = 桌面壳与系统桥接，不再作为正式业务 authority
  - 现役文档已开始同步收口到新口径：
    - HTTP 对齐前不再允许扩“简化版旁路”
    - 项目数据不再默认认本地 `project-store`
    - HTTP 一旦对齐，对应 IPC 必须物理退场

- 当前新增进展（2026-04-18 前后端分离终极收口行动·阶段 2）：
  - PocketBase 项目核心表结构已落迁移：
    - `projects`
    - `project_chats`
    - `project_outlines`
    - `project_characters`
    - `project_detailed_outlines`
    - `project_scripts`
  - 当前云端建模策略已定：
    - 主表 `projects` 存项目元信息、`storyIntent`、`entityStore`、`generationStatus`、可见结果与正式放行状态
    - 各阶段独立分表存 JSON blob，不再把整份项目快照硬塞一张大表
    - 每张阶段表固定 `project` 唯一索引 + `version` 整数版本号
  - 当前明确不做的事：
    - 这轮还没开始 Repository 读写拼装
    - 这轮还没改前端 API
    - 这轮还没删 IPC

- 当前新增进展（2026-04-18 前后端分离终极收口行动·阶段 3）：
  - 已新增 PocketBase 项目仓储层：
    - `server/src/infrastructure/pocketbase/project-repository.ts`
    - `server/src/infrastructure/pocketbase/project-snapshot-mapper.ts`
  - 当前快照拼装口径已落地：
    - `projects + 5` 张阶段表
    - 统一缝成前端认的 `ProjectSnapshotDto` 形状
    - 前端暂时还没接线，但服务端返回口径已经对齐
  - 已补最小乐观锁口径：
    - `projects.projectVersion`
    - 各阶段子表 `version`
    - 保存时按 `expectedVersion` 检查版本冲突，冲突直接抛 `ProjectRepositoryConcurrencyError`
  - 已补本地 PocketBase 迁移脚本：
    - `npm run migrate:up`
    - `npm run migrate:down`
  - 本地 PocketBase 已实际执行 migration 并确认存在 6 张项目表
  - 已补服务端编译验证并通过：
    - `npm run build`

- 当前新增进展（2026-04-18 前后端分离终极收口行动·阶段 4）：
  - 已新增正式项目 HTTP 路由：
    - `server/src/api/routes/projects.ts`
    - 已挂 `authMiddleware`
    - 项目查询与删除都按 `userId + projectId` 做隔离
  - 已补前端统一项目 API 客户端：
    - `src/renderer/src/services/api-client.ts`
    - 已新增项目列表、创建、读取、删除、各阶段保存接口
  - 已完成第一批前端读链换轨：
    - `src/renderer/src/features/home/ui/useHomePageActions.ts`
    - `src/renderer/src/app/hooks/useWorkspaceProjects.ts`
    - `src/renderer/src/app/services/stage-session-service.ts`
    - 项目大厅列表 / 创建 / 删除 / 打开项目初始化，现已改走 HTTP
  - 已修掉两处覆盖风险：
    - 保存 `runtime-state` 时不再清空已有 `scriptDraft`
    - 保存 `detailed-outline` 时不再清空已有 `detailedOutlineBlocks`
  - 当前已确认：
    - `server` 端 `npm run build` 通过
    - 根仓 `npm run typecheck:web` 仍有旧七问面板与未用变量报错，但当前阶段换轨文件未命中新增报错

- 当前新增进展（2026-04-18 前后端分离终极收口行动·阶段 5）：
  - 已新增七问确认正式 HTTP 落盘接口：
    - `POST /api/projects/:projectId/seven-questions/confirm`
    - 服务端现在负责把确认版七问写入 `project_outlines.outlineDraftJson -> outlineBlocks[].sevenQuestions`
  - 七问确认保存已接入项目仓储：
    - 先取当前项目快照
    - 基于现有 `outlineDraft` 或项目基础信息构建 `outlineDraft`
    - 再通过 `saveOutlineDraft` 落盘并触发 `project_outlines.version` 递增
  - 前端七问确认保存已切到 HTTP：
    - `src/renderer/src/features/seven-questions/api.ts`
    - `src/renderer/src/features/seven-questions/ui/SevenQuestionsReviewPanel.tsx`
  - 七问保存后已不再走本地 IPC 回读：
    - 前端直接依赖服务端返回的最新项目快照判断“是否真的落盘成功”
  - 当前已确认：
    - `server` 端 `npm run build` 通过
    - 七问页内 `saveConfirmedSevenQuestions/getProject` 残留 IPC 调用已清零


- 当前新增进展（2026-04-18 前后端分离终极收口行动·清扫战场与 UI 阅兵）：
  - 已补齐扣费账本埋点：
    - `server/src/services/credit-service.ts` 的 `api_call_logs` 现在会写 `project` 和真实 `durationMs`
    - `server/src/api/routes/outline-characters.ts` 已把真实 `projectId` 透传进扣费 metadata
  - 已补前端生成后余额刷新：
    - `src/renderer/src/features/seven-questions/api.ts` 的“生成粗纲和人物”已统一走 HTTP
    - `src/renderer/src/app/hooks/useOutlineCharacterGeneration.ts` 成功后会刷新右上角积分
  - 已拔掉粗纲页 / 人物页两处只读旧 IPC：
    - `src/renderer/src/features/outline/ui/OutlineStage.tsx`
    - `src/renderer/src/features/character/ui/CharacterStage.tsx`
    - 两页现在统一吃 workflow store 里的 HTTP 项目快照 `entityStore`
  - 已补服务端人物底账缝合：
    - `server/src/application/workspace/build-outline-character-entity-store.ts`
    - 生成粗纲/人物后，服务端会尝试把 faction matrix + V2 人物拼成 `entityStore`，并写回 `projects.entityStoreJson`
  - 已补 Electron UI 阅兵脚本：
    - `tools/e2e/observation/ui-outline-character-http-parade.mjs`
    - 脚本现已对齐当前人物页结构，按 `完整人物小传 + 轻量人物卡` 校验总人物数
  - 当前真实状态：
    - UI 读链和扣费刷新链已接通
    - 但最新一次真实联调仍被服务端 `character_contract_incomplete` 门禁拦住，尚未达到”稳定可阅兵”
    - 当前失败不是前端假象，而是后端真实保存未放行

- 当前新增进展（2026-04-19 Phase 7·详细大纲 HTTP 迁移）：
  - 已将详细大纲生成链从 `src/main` 迁移到 `server/`：
    - `server/src/application/workspace/detailed-outline-service.ts` — 正式服务入口
    - `server/src/application/workspace/generate-detailed-outline-support.ts` — 四段生成、批次、normalize、门禁
    - `server/src/shared/domain/workflow/episode-count.ts` — 集数推导
    - `server/src/shared/domain/workflow/outline-episodes.ts` — 粗纲集形状归一化
  - 已挂路由 `POST /api/generate/detailed-outline`，扣 5 积分，失败不扣
  - Fail-closed 门禁 `isDetailedOutlineModelResultComplete` 已在服务层绝对拦截
  - `npx tsc --noEmit` 零错误，服务器启动验证路由返回 401（认证生效）
  - **防弹装甲已生效**：episode control agent 加上 explicit card count warning，missing episode 时使用 `buildEpisodeControlCard()` fallback，不再 fatal throw
  - **15分钟超时已解除**：`resolve-ai-stage-timeout.ts` 中 detailed outline timeout 从 300s 扩到 900s（15分钟），`server/src/index.ts` 中 `server.setTimeout(900000)` 同步调整
  - **实弹验证已通过**（2026-04-19）：
    - 时间：290.2s（在 15分钟窗口内）
    - Credits：100 → 95（扣费 5 积分，失败不扣已验证）
    - Gate：通过（4 segments, 10 episodeBeats）
    - Database：持久化成功（`project_detailed_outlines` 表写入）
  - **PocketBase JSON 字段修复**：`detailedOutlineBlocksJson` 和 `detailedOutlineSegmentsJson` 传对象直接，不需要 `stringifyJson()` wrapper
  - **当前待验证**：剧本生成 HTTP 化（Phase 8）—— Phase 8.1 已完成，待继续 Phase 8.2 调度器迁移

- 当前新增进展（2026-04-19 Phase 8·剧本 HTTP 化·第一梯队基建）：
  - **HTTP 路由骨架已铺设**：
    - `server/src/api/routes/scripts.ts` 已建好 Start/Pause/Resume/Stop/Rewrite 五个核心路由
    - 所有路由挂载 auth 中间件（`authMiddleware`）
    - 积分检查前置（每集 1 积分，余额不足返回 402）
    - `server/src/index.ts` 已注册路由：`app.use('/api/script-generation', scriptsRouter)`
  - **Dummy Worker 异步脱壳已验证**：
    - Start 调用立即返回 202 Accepted（耗时 <500ms）
    - 后台 Worker 每 3 秒模拟完成一集，递增 `completedEpisodes`
    - 前端轮询 `GET /api/script-generation/status/:projectId` 可见进度条移动
    - Pause/Resume 流程完整：暂停后进度停止，恢复后继续递增直至完成
  - **进度板合同已在服务端**：
    - `server/src/shared/contracts/script-generation.ts` 包含完整 DTO 定义
    - `ScriptGenerationProgressBoardDto`（episodeStatuses + batchContext）
    - `ScriptEpisodeStatusDto`（episodeNo, status, batchIndex, reason）
    - `ScriptBatchContextDto`（batchSize, currentBatchIndex, status, updatedAt 等）
  - **PocketBase 进度板持久化已接通**：
    - `project_scripts.scriptProgressBoardJson` 每集完成时实时写入
    - `ProjectRepository.saveScriptState()` 已修复：JSON 字段直接传对象，不需要 `stringifyJson()` wrapper
    - 测试验证：10 集任务完成后 DB 记录 version=7，board.status='completed', episodeStatuses.length=5
  - **验收检查全部通过**：
    - ✓ Start 返回 202
    - ✓ Start 快速返回 (<500ms)
    - ✓ 进度板有 episodeStatuses
    - ✓ 轮询能看到进度递增
    - ✓ 最终完成 (N/N)
    - ✓ Pause/Resume 流程正常

- **新增进展（2026-04-19 Phase 8.2·V8 引擎迁移到服务端）**：
  - **真实 V8 剧本生成引擎已从 Electron 迁移到 `server/src/application/script-generation/`**：
    - 保留完整 Agent-first 架构：`episode-engine-agent.ts`/`arc-control-agent.ts`/`emotion-lane-agent.ts` 全部迁移
    - 保留完整质量 gate：`inspectScreenplayQuality.ts`/`char-count-agent.ts`/`format-pollution-agent.ts` 全部迁移
    - 保留完整 batch 逻辑：每 5 集一批，batch 内最多 3 次尝试，episode 级 repair，单集失败不阻断整季
  - **`server/src/api/routes/scripts.ts` 已重写，Dummy Worker 替换为真实 `startScriptGeneration()` orchestrator**：
    - POST /start 触发真实 AI 生成，积分按实际完成集数扣费，失败不扣
    - MAX_TARGET_EPISODES = 2 安全 cap 已就位，避免误生成超长任务
  - **真实单集生成验证已通过**：
    - 1 集生成耗时 54 秒，产出 1469 字真实 screenplay，包含陆烬/韩枭对话
    - 字数、场次、格式全部符合正式合同
    - 积分从 100 → 99，扣费记录在 PocketBase `api_call_logs`
- **新增进展（2026-04-19 Phase 8.3·前端 HTTP 迁移完成）**：
  - **`src/renderer/src/features/script/ui/useScriptStageActions.ts` 已完全重写**：
    - `handleStartGeneration`：从 IPC 调用完全切换为 HTTP POST /api/script-generation/start + 3 秒轮询
    - `handleRewriteEpisode`：从 IPC 调用切换为 HTTP POST /api/script-generation/rewrite/:episodeNo + 轮询刷新
    - `handleAutoRepair`：从 IPC 调用切换为 HTTP GET refresh，刷新项目快照
    - 文件从 464 行精简到 ~340 行，移除所有旧 IPC 依赖，零编译错误
  - **`src/renderer/src/services/api-client.ts` 已新增剧本生成完整 HTTP 接口**：
    - `startScriptGeneration`/`pauseScriptGeneration`/`resumeScriptGeneration`/`stopScriptGeneration`
    - `getScriptGenerationStatus`/`rewriteEpisode`
  - **HTTP 全链路实弹验证已通过**：
    - POST /start → 202 Accepted 立即返回
    - 18 次轮询（3 秒/次）后，任务从 running → completed
    - 最终返回 2 集真实 AI 生成 screenplay，格式、字数、质量全部符合合同
    - 积分从 100 → 99，扣费记录完整，事务可查
    - 进度条由轮询数据驱动，完全准确，没有本地估算偏差
- **当前结论**：
  - 剧本生成完整 HTTP 链路已完全打通，前后端 IPC 链路已完全退场
  - 积分扣费逻辑正常，按实际完成集数扣费，失败不扣
  - 生成质量与原有 Electron 链路完全一致，所有 Agent、gate、质量控制全部生效
  - 前端完全使用 HTTP 接口，不再依赖任何 `window.api.workspace` 剧本相关 IPC 调用

## 当前判断

1. 当前仓库的本质问题已经不是“有没有 HTTP”，而是“HTTP、PocketBase、本地 IPC 三套权力还没收成一套”。
2. 阶段 2 现在已经把 PocketBase 数据骨架钉住了；后面阶段 3 可以直接围绕这 6 张表做 Repository，不用再边写边猜存储结构。
3. 当前正式项目真相已明确走“主表 + 分阶段子表 + 每表独立 version”的方案，不再沿着本地 `projects.json` 那种整包快照思路继续长。
4. 阶段 3 已把“云端怎么读、怎么写、怎么拼快照”落到服务端；阶段 4 已开始把前端项目 CRUD 和工作台初始化读链切到 HTTP。
5. 当前阶段 5 已把七问“确认并保存”这条前置真相链收成 HTTP 单线；七问确认后，前端已开始只认服务端快照，不再自己本地盲改再补一次 IPC。
6. 当前还没动粗纲/人物正式生成链；这轮只确认“七问作为前置弹药”已经变成纯云端真相。
7. 现在的验收第一优先级不再是单个页面能不能跑，而是：正式业务入口、正式存储入口、旧业务壳，三者边界有没有写死。

## 下一终端先看什么

1. 先按现役顺序读：`AGENTS.md` -> `docs/聊天记录.md` -> `docs/项目原则.md` -> `docs/AI自我学习.md` -> `docs/项目结构.md` -> `docs/主线/2026-04-18-主线.md`
2. 如果继续做阶段 2，先看：
   - `server/pb_migrations/`
   - `server/src/infrastructure/pocketbase/`
   - `src/shared/contracts/project.ts`
   - `src/shared/contracts/workspace.ts`
3. 如果继续做阶段 3，先看：
   - `server/pb_migrations/1776471001_created_projects.js`
   - `server/pb_migrations/1776471002_created_project_chats.js`
   - `server/pb_migrations/1776471003_created_project_outlines.js`
   - `server/pb_migrations/1776471004_created_project_characters.js`
   - `server/pb_migrations/1776471005_created_project_detailed_outlines.js`
   - `server/pb_migrations/1776471006_created_project_scripts.js`
   - `src/main/infrastructure/storage/project-store.ts`
   - `src/main/infrastructure/storage/project-store-core.ts`
   - `server/src/infrastructure/pocketbase/client.ts`
4. 如果继续做阶段 4，先看：
   - `server/src/infrastructure/pocketbase/project-repository.ts`
   - `server/src/infrastructure/pocketbase/project-snapshot-mapper.ts`
   - `server/src/api/routes/projects.ts`
   - `src/renderer/src/services/api-client.ts`
   - `src/renderer/src/app/hooks/useWorkspaceProjects.ts`
   - `src/renderer/src/features/home/ui/useHomePageActions.ts`
   - `src/renderer/src/app/services/stage-session-service.ts`
5. 不要再把 `src/main/ipc` 和 `src/main/infrastructure/storage` 当长期正式主链，只能把它们视为待退场旧壳
