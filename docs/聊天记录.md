# 聊天记录

## 永久记忆

- 这是 AI 剧本创作软件，不是普通文本生成器。
- 正式主链固定：用户输入 → 确认信息 → `storyIntent` → 粗纲 → 人物小传 → 详细大纲 → 剧本。
- 当前工程主权固定：
  - renderer 只做展示和交互
  - server 是唯一正式业务入口
  - PocketBase 是唯一正式真相存储
  - IPC 只保留桌面壳职责

## 长期记忆

- 前后端分离已经完成主链收口，旧 IPC 业务链理论上应全部退场。
- 多项目共用同一个阿里云 PocketBase，必须靠 `appId` 和表前缀做隔离。
- 第三方模型通道优先；只有第三方不可用时才回退官方 DeepSeek。
- 质量门结论只认当前代码复跑结果，不认旧截图、旧口头结论。

## 短期记忆

- 2026-04-22：上一轮 `test/lint/build` 三关已同绿并正式封口。
- 2026-04-22：用户重定文档标准，现役入口改为 `CLAUDE.md + AGENTS.md 指针 + 文档管理.md`。
- 2026-04-22：`docs/` 已重组为 5 个活文档 + `当前主线.md` + `archive/`。
- 2026-04-22：后续所有文档整理统一按新标准执行，旧 `docs/归档/`、`docs/主线/` 口径退场。
- 2026-04-22：HTTP/IPC 双口径清理完成。正式业务读写全部走 HTTP api-client，IPC 只保留桌面壳能力（事件订阅、导出、系统信息、运行时控制）。
- 2026-04-22：validateStageInputContract 假迁移收口完成——从 main 内联版改为 src/shared/domain/workflow/validate-stage-input-contract.ts 共享模块，server 和 main 共用同一真相源。
- 2026-04-22：删除 6 个 dead 文件：start-script-generation.ts/.test.ts、rewrite-script-episode.ts/.test.ts、script-generation-worker.ts、script-generation-worker-runner.ts。server 已有独立副本，main 端不再保留生成业务逻辑。
- 2026-04-22：execute-script-repair.ts 整条 repair 链删除——server 端 execute-repair 路由仍是 TODO 占位，main 版删掉后 renderer 正式修稿入口已切到 HTTP api-client，但 server 实现待补。
- 2026-04-22：create-script-generation-prompt.ts 仍保留 main 和 server 双份——server 已接管正式生成，main 版被 run-script-generation-batch.ts 本地消费，需继续顺藤摸瓜清整条旧 runtime。
- 2026-04-22：generate-seven-questions-draft.ts 删除——server 已有 /api/generate/seven-questions 路由，main 版 IPC handler 改为 throw 占位。
- 2026-04-22：main 端 script runtime 链整条删除——run-script-generation-batch.ts、create-script-generation-prompt.ts 及全部 runtime/prompt 子目录（约 30 个文件）。server 已有完整独立副本，renderer 正式入口已切 HTTP。main 端不再保留任何剧本生成 authority。

## 当前结论

- 以后开工先读：`AGENTS.md` → `聊天记录.md` → `项目结构.md` → `项目原则.md` → `AI自我学习.md` → `当前主线.md`
- 回答架构问题前先读 `graphify-out/GRAPH_REPORT.md`
- 只要发现第二口径、补丁链、隐藏降级，就必须汇报并优先收口
