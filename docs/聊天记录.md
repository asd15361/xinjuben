# 聊天记录

## 永久记忆

- 这是 AI 剧本创作软件，不是普通文本生成器。
- 正式主链固定：用户输入 → 创作信息总结/故事梗概 → `storyIntent` → 人物小传/底账 → 剧本骨架 → 详细大纲 → 剧本；七问候选只做方案审核辅助。
- 当前工程主权固定：
  - renderer 只做展示和交互
  - server 是唯一正式业务入口
  - PocketBase 是账户与计费真相源；本地存储是内容真相源
  - IPC 只保留桌面壳职责
- 创作架构长期采用 `A+B+C`：稳定创作内核 A + 市场打法包 B + 项目自身设定 C。市场变化优先更新 B，不推翻 A。

## 长期记忆

- 前后端分离已经完成主链收口，旧 IPC 业务链理论上应全部退场。
- 多项目共用同一个阿里云 PocketBase，必须靠 `appId` 和表前缀做隔离。
- 第三方模型通道优先；只有第三方不可用时才回退官方 DeepSeek。
- 质量门结论只认当前代码复跑结果，不认旧截图、旧口头结论。
- **架构边界已钉死**：renderer 发起与展示，main 本地只读辅助，server 正式写入 authority，PocketBase 账户/计费真相源，本地存储内容真相源。
- 用户可见文案统一用“剧本骨架”，不要再把“粗略大纲”作为产品名；内部类型可渐进保留。
- 七问候选是草稿态，必须支持持久化、选择、追加生成和锁定；但人物小传/剧本骨架正式输入以 `storyIntent` 为准，不能再维护第二套账本。
- 回答不再主动汇报上下文百分比或“无法精确读取”；除非用户问上下文状态。

## 短期记忆

- 2026-04-23：`script-audit.ts` /execute-repair 不再写 PB 大 JSON——server 返回 repairedScript + ledger，renderer 待集成写本地。类型检查全绿，测试全绿。
- 2026-04-23：projects.ts 旧写入路由确认——`POST :projectId/script` 和 `:projectId/runtime-state` 仍被 `useWorkspaceProjects` 调用（workspace 手动保存场景），记录为已知第二写入口径，待后续收口。
- 2026-04-23：内容本地真相源迁移封板验证完成——server 不再写 PB 大 JSON，renderer 写本地，类型检查全绿，测试全绿，2 处竞态/提示风险已修复。
- 2026-04-23：**PocketBase text 字段默认 max=5000 字符限制**——scriptDraftJson 序列化后超限导致写入 400 错误。已将 `xinjuben_project_scripts` 的 JSON 字段 max 设为 100000（但剧本正文不再存 PB，走本地持久化）。
- 2026-04-23：剧本生成写入验证通过——scriptDraft count=1，screenplay length=1882。
- 2026-04-22：剧本正式 authority 收口主线完成——八项能力全部归 server，无残留占位。
- 2026-04-22：迁移结论文档定稿——`docs/剧本Authority迁移结论.md`，架构边界钉死。
- 2026-04-22：历史测试债清零——498 tests, 493 pass, 0 fail, 5 skipped。
- 2026-04-22：单集重写真逻辑补实——`server/src/application/script-generation/rewrite/execute-script-rewrite.ts`。
- 2026-04-22：execute-repair 真逻辑补实——剧本修稿 authority 全部收口到 server。
- 2026-04-22：authority 盘点完成——`docs/authority-盘点清单.md`。
- 2026-04-22：truth-authority.ts 和 truth-owner-matrix.ts 同步更新。
- 2026-04-22：project-store.ts 废弃标记。

- 2026-04-24：P4（内容质量检测升级）完成——`inspectContentQualityEpisode` 新增信息密度、剧本格式、男频/女频垂类 5 维度评分，`inspectContentQualityBatch` 支持市场 profile 和批量均分。测试 600+ 全绿。
- 2026-04-24：P5（修稿链响应低分）完成——`buildContentRepairSignals` 将低分转化为结构化修稿信号，`buildContentRepairPrompt` 改问题清单格式，`shouldAcceptRepair` 新增垂类分回退逻辑。测试 603 全绿。
- 2026-04-24：P6（最终验收）完成——男频/女频各 20 集回归测试通过。男频市场匹配度 27、开局冲击 11 偏低；女频市场匹配度 54、开局冲击 14。P0-P6 全链路闭环。
- 2026-04-24：验收结论：CONDITIONAL GO——架构完整、无双口径，但内容质量（开局冲击、爽点兑现、男频市场匹配）需要改进。报告见 `server/docs/P6-final-acceptance-report.md`。
- 2026-04-25：大上下文利用 P4（snapshot 连续性检测）完成——`buildStoryStateSnapshot` + `inspectStoryContinuityAgainstSnapshot` 5 类检测，接入 content-quality 和 postflight。
- 2026-04-25：大上下文利用 P5（收口与可观测性）完成——连续性分数 UI 面板、`storyContinuityScore` 后飞行输出、修稿信号、重写约束全链路闭环。
- 2026-04-25：灵感对话到七问链路修复——区分 `creativeSummary` 和 `storySynopsis`，七问支持候选列表、选择、追加生成、锁定和持久化。
- 2026-04-25：体验收口——七问生成有读秒提示，“粗略大纲”用户可见文案改为“剧本骨架”。
- 2026-04-25：新战略确认——软件要成为学习型创作系统；优秀剧本样本先提炼成可审核、可版本化的 MarketPlaybook，再接入 Prompt 和质检。
- 2026-04-25：P3（MarketPlaybook 合同与资产注册表）完成——`MarketPlaybookDto` + `MarketPatternDto` + 内置注册表 `market-playbook-registry.ts`，19 测试全绿。
- 2026-04-25：P4（MarketPlaybook 版本化与项目锁定）完成——`MarketPlaybookSelectionDto` + `resolveMarketPlaybookSelection` + PB 持久化 + mapper 测试 7 项。
- 2026-04-25：P4.5（PB 字段收口）完成——migration `1776471008_add_market_playbook_selection_json.js` + mapper 测试 7 项。
- 2026-04-25：P5（样本导入与草案提取）完成——`createMarketPlaybookDraftFromSamples` + 6 词典启发式 + `createSourceSample` + 12 测试。
- 2026-04-25：P6（Playbook 生命周期）完成——`validateMarketPlaybookBeforeActivation` + `activateMarketPlaybookDraft` + `archiveMarketPlaybook` + 版本冲突检测 + 16 测试。
- 2026-04-25：P7（可观测性）完成——`buildPlaybookSectionMarkdown` 导出 + `[market_playbook_locked]` 日志 + 7 测试。测试 720 全绿，typecheck 全绿。
- 2026-04-25：**P8（MarketPlaybook Prompt 集成）完成**——B 层接入七问/剧本骨架/正式剧本三阶段 prompt。`buildMarketPlaybookPromptBlock` 阶段特定输出 + 截断限制 + 边界声明。测试 730 全绿（725 pass, 0 fail, 5 skipped），server typecheck 全绿，root typecheck 全绿。隔离验证：`screenplay-content-quality.ts` 和修稿链无 playbook 引用。
- 2026-04-25：**P9（MarketPlaybook 质量观测）完成**——`inspectPlaybookAlignment` 纯关键词匹配，`playbookAlignmentScore` 只观测不驱动修稿。4 条边界测试证明低分不触发 ContentRepairSignal。`shouldAcceptRepair` / `buildContentRepairSignals` 均不引用 playbookAlignmentScore。测试 744 全绿（739 pass, 0 fail, 5 skipped），server typecheck 全绿，root typecheck 全绿。
- 2026-04-25：**P10（MarketPlaybook 低成本闭环验证）完成**——新增 `market-playbook-learning-flow.test.ts`，证明样本→草案→人工审核→启用→Prompt block→playbookAlignmentScore 观测链路可跑通；同时断言传入 playbook 不改变 `overallScore`。测试 746 全绿（741 pass, 0 fail, 5 skipped），server/root typecheck 全绿。
- 2026-04-25：**P11（MarketPlaybook 审核工作台 MVP）完成**——首页新增“市场学习”工作台，可粘贴/导入 `.txt/.md` 样本、生成草案、人工编辑 promptInstruction/qualitySignal、查看 Prompt 预览、复制 active JSON。新增 `review-workbench.ts` + 3 测试。`npm run typecheck`、`npm test`、server typecheck、root tsc 全绿。
- 2026-04-25：人物小传/剧本骨架生成链路收口——人物小传先生成并保留；`rough_outline_batch_parse_failed` 不再整步 500，改为返回可见 warning + 临时骨架；20 集势力编制减负，避免按 60 集规模硬塞人物。
- 2026-04-25：聊天保存核查——项目 `hg7vtux3qnprifq` 后端仍能读到 26 条聊天，刷新后“看不到”不是数据丢失；HTTP 烟测确认新聊天保存写入/读回 2 条通过。
- 2026-04-26：UI 卡顿复发记录——本轮给人物小传/剧本骨架加复制按钮后，用户反馈 UI 明显变卡。已写入工程约束：复制/导出类大文本必须点击时惰性构建，UI 改动必须补真实页面滚动、输入、展开/复制体验检查，不能只看 typecheck。
- 2026-04-26：UI 卡顿修复落地——`CopyTextButton` 去掉每实例 React state，改为点击时 DOM 反馈；`CharacterStage` 移除 `framer-motion layout` 卡片动画；`OutlineBasicsPanel` 拆出 memo 分集卡，避免输入一集时所有 textarea 重算高度。新增 3 条性能回归测试，`npm test` 771 项全绿。
- 2026-04-26：UI 卡顿二次定位——用户描述刷新后“我的项目”约 5 秒才出现、侧边导航每次点击等 3-5 秒。根因是列表/阶段切换等待远端项目接口后才更新 UI。已新增项目摘要 `localStorage` 缓存、阶段切换乐观更新和 3 条性能回归测试，`npm run typecheck` 通过。
- 2026-04-26：Hook 崩溃修复——首页缓存逻辑保持单个 `useAuthStore` hook，用户 id 改从 store 快照读取，避免 HMR/错误边界下触发 `Rendered more hooks than during the previous render`。
- 2026-04-26：人物小传质量链路修复——针对真实样本里 `林霄/陆渊` 双主角、兜底模板话泄漏、20 集 10 人全升完整且轻量卡为 0 的问题，已加主角名锁、兜底文案清理、20 集完整小传上限 8 和定向回归测试。状态：工程验证通过，但用户还没重新生成实测；下一步先测人物质量，过关后再继续骨架。
- 2026-04-26：人物小传二次实测未过——真实输出仍是完整人物 8、轻量人物卡 0，且李寒/林清/慕容雄泄漏“职责边界/规矩解释权”同模板。已修：短剧势力空席也转轻量卡；通用补全按爪牙、忠诚卧底、家族合作者分流。定向测试和 server typecheck 通过，待用户重新生成实测。
- 2026-04-26：人物小传第三轮实测继续收口——轻量卡已到 6，说明空席转轻量卡生效；剩余问题是 V2 小传拼接 `。，/。。`、轻量卡想要/压力重复、忠诚卧底模板带出项目外 `慕容家`、`表面是苏婉柔是`、情感人物泛化句。已修并加定向测试，server typecheck 和 health 通过，待用户重生验证。
- 2026-04-26：豆包反馈追加收口——豆包判断结构进步但文本质量仍有问题，基本可信但评价偏夸。已继续修功能席位同质化：护法/弟子/情报/执行等轻量卡 goals 按职责生成。定向测试 10 条通过，server typecheck 和 health 通过。
- 2026-04-26：人物生成规则升级——采纳豆包深层诊断，问题根源是内容生成 Prompt/字段映射/弧线定义。已改 `character-profile-v2-agent`：自然 biography、具体可拍 conflictTrigger、可入剧本 advantage、起点/触发/摇摆/代价/终局 arc。解析层和 shared 映射会二次规整硬拼小传与结果式弧线。13 条定向测试、server typecheck、root typecheck、health 通过。
- 2026-04-26：人物规则深挖——继续按“设定表→能写戏的人物底账”改后端/共享规则：biography 必须融合五维并带人物名，未结构化 arc 一律重写为起点/触发/摇摆/代价/终局；enrich 兜底弧线同步结构化；共享 slot 工具去掉“撑住这条线”；复制完整小传带出外在形象/性格/身份/价值观/剧情作用。全量 `npm test` 781 条 0 失败，root build 通过。
- 2026-04-26：人物归属串位修复——用户新测输出显示正文变强，但底账仍串：完整人物“盟主/慕容雪/张天师”重复进轻量卡，且仙盟人物串入青虚宗。已修 `build-outline-character-entity-store`：按 V2 `factionId` 做主归属裁决、跳过错误势力里的同名占位、同名实体合并、缺失完整 profile 补回主势力；`derive-character-stage-sections` 按同名兜底隐藏已升完整人物；势力矩阵 Prompt 禁止跨势力复制同一人物名。全量 `npm test` 782 条 0 失败，root build、server/root typecheck、health 通过。
- 2026-04-26：P0 生成链路重构第一刀——不再继续只修表层文案，先给人物小传/剧本骨架链路建立 canonical bundle 边界：`OutlineCharacterGenerationBundleDto` 统一承载 `storyIntent`、骨架、七问、人物底账、diagnostics、warnings；旧 `generateOutlineAndCharactersFromConfirmedSevenQuestions` 只作为兼容 wrapper 返回旧结构。
- 2026-04-26：人物合同收紧——`getCharacterContractIssues` 从“旧字段或 V2 字段满足其一即可”改成“旧展示字段和 V2 账本字段都必须满足”，避免半成品人物继续向下游传递。新增 bundle 回归测试锁住骨架失败时人物底账不丢、warning/diagnostic 不静默。
- 2026-04-26：修复真实重生阻断——用户实测报 `character_profile_v2_parse_failed:青云宗:json_parse_failed`，说明 AI 已返回人物但 JSON 外壳不合法时后端仍 500。已修 `character-profile-v2-agent`：解析器可从 fenced/prose 响应中提取 JSON；连续坏 JSON 后按势力占位生成 fallback V2 人物并记录 `faction_parse_fallback`，不再阻断骨架。
- 2026-04-26：人物链路质量 P0 收口——用户新测证明链路已不 500，但仍泄漏模板句、箭头弧线整段复制、轻量卡想要/压力重复、慕容管家串入主角宗门。已修 shared V2 biography/arc 映射、server enrich 规则兜底、entityStore 同族管家归属纠偏、renderer 轻量卡预览去重；定向测试 20 条、root/server typecheck、全量 `npm test` 784 条、`npm run build` 全绿。
- 2026-04-26：生成耗时翻倍根因定位并修复——真实日志显示第一轮已完成人物/骨架尝试，但 `storyIntent.protagonist=主角`、实际人物名为 `叶辰`，合同误判 `protagonistCovered=0`，触发整链 retry，导致 400 多秒变 800/900 秒。已修：泛称主角不再触发硬覆盖校验；人物坏 JSON 单人补救并发从 1 提到 2；已中断跑偏请求并重启后端。
- 2026-04-26：人物链路真实输出继续收口——用户新贴样本暴露 `陆青瑶` 被套进“旧账/外部靠山”模板、`王岳` 被 fallback 写成“被更强的人替掉”、`柳长老` 被误套“养育恩情”、`林夜` 弧线标签重复、`云天鹤/柳长老` 串入玄天宗。已修：守护女主/亲传大弟子专属补全分支、仙盟爪牙不再走收养忠诚模板、fallback 占位人物改自然压力、弧线阶段标签清洗、按 identity/biography 文本纠偏 faction 主归属、骨干席位去掉“撑住”口径。新增 5 类回归，定向 25 条通过，全量 `npm test` 786 条 0 失败，root/server typecheck、root build、health、前端 HTTP 200 通过。
- 2026-04-26：题材策略层升为 P0——用户确认担心玄幻规则调好后污染女频/都市/律政。已完成架构排查与设计草案：新增 `docs/design/001-generation-strategy-layer.md`，并把 `GenerationStrategy` 重构计划写入 `当前主线.md` 顶部。结论：底层人物/骨架引擎通用，题材规则必须按 audienceLane/subgenre 插件化；玄幻硬编码要迁入 `male_xianxia`，并新增女频/都市律政策略与污染检测。
- 2026-04-26：P0.1 题材策略层落地——新增 `src/shared/domain/generation-strategy/generation-strategy.ts` 和测试：六个内置垂类从 `marketProfile` 解析到策略，旧项目可从 `genre` 兜底，都市律政作为试点策略，污染词检测可拦 `宗门/仙盟/魔尊血脉` 等串味。`buildMarketProfilePromptSection` 已开始注入策略层词库、人物位、势力席位、阶段规则和禁用词。验证：root/server typecheck、策略测试、server prompt section 测试、全量 `npm test` 793 条通过。
- 2026-04-26：P0.2 玄幻硬编码第一轮迁移——`faction-matrix-agent` 不再自己判断隐藏血脉修仙，改由策略层渲染；主角 mandatory fallback 改走 `buildStrategyProtagonistFallback`；人物补全把 `血脉/仙盟/宗门` 等核心词改为策略上下文；正式剧本场景指令新增策略入参，非玄幻题材走通用策略词库，不再吃全局长老/法阵/宗门/妖兽指令。验证：root/server typecheck、定向 13 条、全量 `npm test` 798 条通过。
- 2026-04-26：P0.3/P0.4 第一轮题材回归——粗纲总纲/分集 Prompt 接入策略层，非玄幻项目不再吃全局妖兽/法阵/封印/山门/修炼/悟道规则；`buildMarketProfilePromptSection` 对非玄幻不再把禁用词原样塞进 Prompt，只保留策略边界说明；新增女频霸总主链漏主角兜底回归、都市律政粗纲和正式剧本场景回归。验证：策略层 12 条、相关定向 50 条、主链 6 条通过。
- 2026-04-26：正式剧本 Prompt 第一轮题材分流——`create-script-generation-prompt` 的最终几集规则、终集开场规则、结尾集合同已改为策略感知；非玄幻不再强吃“法阵/封印/宗门/残党/伤势”等玄幻收束口径。`build-ledger-prompt-blocks` 的战术替代表达也分流：玄幻可用法阵/妖兽/血脉，都市/女频/律政改用口供、监控、录音、转账记录、关系证据等现实证据链。相关定向 61 条、server typecheck 通过。
- 2026-04-26：详细大纲与污染 warning 第一轮策略接入——`generation-stage-prompts` 详细大纲 Prompt 不再对非玄幻项目输出宗门/仙盟/妖兽/法阵/山门/悟道等旧规则，改为按策略词库约束机构、证据、合同、关系和现场动作。人物/骨架 canonical bundle 新增生成后题材污染 warning：女频霸总输出中若混入宗门/仙盟，会返回 `generation_strategy_contamination`。相关 15 条定向测试、server typecheck 通过。
- 2026-04-26：P0 题材策略继续收口——正式剧本 postflight 接入 `generation_strategy_contamination`，女频/都市/律政成稿若混入宗门/仙盟/魔尊血脉等当前策略禁词会写入 postflight issues；控制卡 `requiredProp` fallback 改从 `GenerationStrategy.worldLexicon.conflictObjects` 取道具，霸总不再轮到令牌/血书；Electron IPC 的旧人物/骨架/详纲生成入口已退役为 HTTP API 权威入口，防止未来绕回 `src/main` 旧提示词副本。
- 2026-04-26：P0.8 main 旧生成链物理退场——`src/main` 已删除人物小传/剧本骨架/详细大纲旧 IPC handler、旧 prompt、旧 agent、旧骨架实现、旧测试和零引用 DTO；renderer 生成继续走 HTTP `api-client`，server 是唯一人物/骨架/详纲 authority。
- 2026-04-26：P0.9 server 本地 shared 副本物理退场——`server/src/shared/` 已删除，server 的 `@shared/*` 统一指向根 `src/shared/*`；人物合同、V2 小传映射、控制卡、题材策略不再有 server 本地第二口径。
- 2026-04-26：P0 题材污染自动清洗第一刀——`GenerationStrategy` 新增 `repairStrategyContaminationText`；人物/骨架 canonical bundle 在下传前清洗 `outlineDraft`、人物底账和 `entityStore`，女频/都市/律政串入宗门/仙盟/血脉时先替换为当前题材词，再输出 `generation_strategy_contamination_repaired`，残留才报 `generation_strategy_contamination`。
- 2026-04-26：P0 题材污染自动清洗第二刀——`GenerationStrategy` 的清洗能力上移为可递归处理任意生成值，人物/骨架 bundle 复用共享函数；详细大纲在四幕合并并通过完整性校验后统一清洗 `DetailedOutlineSegmentDto[]`，女频/都市/律政详纲里残留的宗门/仙盟/魔尊血脉会在返回前替换为当前题材词，并写 diagnostic。
- 2026-04-26：正式剧本题材污染前移——`strategy_contamination` 已进入 `EpisodeGuardFailure`，`run-script-generation-batch` 每集解析后会按当前 `GenerationStrategy` 检测禁词，污染稿触发 `retry_parse` + `episode_rewrite`，问题单明确要求“只保留当前题材”；postflight 保留最终兜底审计，不再新增第二裁判口。
- 2026-04-26：真实修仙人物样本第四刀收口——字段硬拼小传（`身份是/性格底色/在戏里`）、结构化弧线终局整链重复、张强类压迫者误进“旧账/外部靠山”兜底、组织骨干位“自己这条线”口径已加源头修复和定向回归；后续先让用户重新生成人物小传，再推进骨架。
- 2026-04-26：真实修仙人物样本第五刀收口——用户新测周云鹤仍是通用程序腔（基本秩序/现场关系/现场后果/程序慢半拍）。已新增“野心大长老/夺血脉夺权”专属补全，并收紧判定避免李寒这类“大长老爪牙”被误判成大长老本人。人物定向 30 条、root/server typecheck 通过。
- 2026-04-26：真实修仙人物样本第六刀收口——用户新测仍暴露三类残留：弧线 `起点：起点是/代价选择→终局` 重复、小传自然段出现 `让X信奉/在主线里的作用/行动抓手`、男性名 `林尘` 被女情感杠杆兜底写成 `她/师妹/少女`。已修 V2 映射层弧线标签/箭头终局清洗、自然 biography 融合句式、男性名性别错位保护和情感兜底显式人名化。全量 `npm test` 758 条 0 失败，root/server typecheck 通过；顺手修正一个存储迁移测试自身“默认跳过扫描”却显式传参的口径错误。
- 2026-04-26：测试账号补积分与充值弹窗修复——已给 `12345@qq.com` 的 `xinjuben` 钱包补 1000 积分，余额从 1 到 1001。充值弹窗被人物小传大面板压住，已改为 `createPortal(document.body)` + `z-[99999]` + body 滚动锁。发现 `xinjuben_transactions` 线上 schema 与代码预期不一致，流水写入被约束挡住，待后续单独修 schema/流水口径。
- 2026-04-27：按豆包对照 123.txt 的反馈修人物小传验收面——采纳“五维主视图缺失、主角不应炸毛、反派不能只靠喊打喊杀、阵营多但场景要复用”的有效部分。已改：完整人物卡主视图展示外在形象/性格/身份/价值观/剧情作用；复制文本和 UI 把 `一碰就炸` 改为 `被逼动作点`；人物 V2 Prompt 强化主角不窝囊不崩溃、反派靠权力/规则/布局压人；势力矩阵 Prompt 加 20 集 3-5 个高复用可拍场景约束。验证：人物/阵营定向 45 条、root/server typecheck 通过。
- 2026-04-27：继续采纳豆包 3 条架构建议——人物池按集数绑定预算（20 集 3+3+5、60 集 5+5+10）、功能位新增 `reusableRoleKey/reuseSceneKeys` 跨场景复用、核心人物新增 `payoffTags` 绑定短剧爽点。已打通 shared 合同、势力矩阵 Prompt、人物 V2 Prompt/fallback/RAG、人物页复制文本和主视图展示；定向人物/阵营/前端复制测试通过。
- 2026-04-27：爽点全链路标准化 V1 收口——`payoffTags` 已从人物底账打通到调度表、骨架、详细大纲、正式剧本控制卡和 `ScriptSegment`，结构字段统一归一到 16 种标准爽点名；`规则反噬/挑衅反杀/底牌隐藏` 等别名会被标准化。5 集男频修仙小批量验收通过：字段四层一致、正文演出爽点、无英文残留、无主角崩溃乱冲。

## 当前结论

- 以后开工先读：`AGENTS.md` → `聊天记录.md` → `项目结构.md` → `项目原则.md` → `AI自我学习.md` → `当前主线.md`
- 回答架构问题前先读 `graphify-out/GRAPH_REPORT.md`
- 只要发现第二口径、补丁链、隐藏降级，就必须汇报并优先收口
- **剧本正式写入 authority 八项能力全部归 server，架构边界已钉死，作为后续所有主线基准**
- **P8 完成：MarketPlaybook B 层已接入三阶段 prompt，隔离边界明确**
- **P0 人物/骨架链路进入重构期：先固定 bundle 合同与模块边界，再继续提示词质量优化**
- **P9 完成：`playbookAlignmentScore` 纯观测信号，不进 overallScore，不触发修稿，4 条边界测试验证隔离**
- **P10 完成：MarketPlaybook 学习闭环已有低成本纯函数验证，未跑真实 API**
- **P11 完成：MarketPlaybook 审核工作台可用，但 active JSON 仍是预览资产，尚未数据库化**
- **人物小传/剧本骨架链路已改为 StoryIntent 直驱；七问只保留候选/审核价值，不再作为硬前置账本**
- **当前下班状态：人物生成规则升级、人物归属串位修复、坏 JSON 恢复、模板句/弧线/轻量卡重复/同族管家串阵营修复、泛称主角误触发整链重跑修复、守护女主/亲传大弟子/faction 文本纠偏、字段硬拼 biography 拦截、张强/普通执事兜底源头清理、野心大长老分支、弧线阶段标签/箭头终局清洗、自然小传机器句拦截、男性名性别错位保护都已加载；题材策略层 P0.1/P0.2 已实现，P0.3/P0.4 第一轮题材回归已实现，详细大纲、正式剧本主 Prompt、ledger 约束、控制卡 fallback 已完成第一轮策略分流；人物/骨架 bundle 和详细大纲出口已接题材污染自动清洗，正式剧本 batch guard/retry 已接入 `strategy_contamination`，postflight 仍保留最终兜底审计；Electron IPC 旧生成入口、`src/main/application/workspace` 已退役旧提示词文件、`server/src/shared` 本地副本均已物理删除。下一步先真实重生人物小传，再确认是否推进骨架。**
- **最新验证边界：`cd server && npm run build` 仍被既有 `.ts` 后缀 import 配置挡住；server typecheck、root typecheck、root build 和全量测试已通过。**
- **MarketPlaybook 当前状态：Prompt 集成 ✅ → 质量观测 ✅ → 低成本闭环验证 ✅ → 审核工作台 ✅ → 自定义打法包持久化 ❌ → 修稿链未接入 ❌**
- **UI 性能是当前显性风险：以后前端改动必须做真实页面卡顿验证，重点盯 `CharacterStage`、`OutlineStage`、首页工作台和复制/导出类能力。**
