# 聊天记录

这个文件只保留一条当前主线。

断线恢复时，先看 `AGENTS.md`，再看这份文件。

## 当前主线

**2026-04-21 天网修复令完成**：
- Shared 双轨制彻底消灭：`server/src/shared/` 已删除，server 通过 `@shared` 别名统一引用工程根 `src/shared/`
- 编译三线零错误（server/web/node）
- 架构真理源唯一：前后端只认一份 `src/shared/`

**2026-04-20 阿里云部署完成**：
- PocketBase 0.36.9：`http://47.95.179.60/pb/`，Admin：`admin@xinjuben.com / xinjuben2026`
- 全局共享表：`users`/`user_apps`/`user_wallets` + 系统表
- xinjuben 业务表：`xinjuben_projects`/`_chats`/`_outlines`/`_characters`/`_detailed_outlines`/`_scripts`/`_transactions`
- API Rules：`@request.auth.id != "" && user = @request.auth.id`
- xinjuben-server：端口 3001，Nginx 反向代理 `http://47.95.179.60/api/`
- 详细配置见 `docs/部署配置.md`

**部署口径收口**：
- 现役只认阿里云，旧 Cloudflare/Vercel/Sealos 口径已退场
- 支付统一支付宝当面付，回调 URL 必须用阿里云域名

**净网除尘令完成**：
- `server/src/main/`/`preload/` 已删除（Electron 代码副本）
- `src/preload/api/` 业务模块已退场，仅保留 `system.ts`/`workspace.ts`
- 图谱节点：3245→2582，陈旧节点已清零

## 前后端分离历史进展（已完成）

**Phase 1-3（PocketBase 建模）**：
- 主表 + 5 张阶段子表，每表独立 version
- `project-repository.ts` + `project-snapshot-mapper.ts`
- 乐观锁：版本冲突报 `version_conflict`

**Phase 4（HTTP 路由）**：
- `server/src/api/routes/projects.ts` + `api-client.ts`
- 前端读链换轨：项目 CRUD 统一走 HTTP
- 覆盖风险修复：保存时不清空已有字段

**Phase 5（七问 HTTP）**：
- `POST /api/projects/:projectId/seven-questions/confirm`
- 前端只认服务端快照，IPC 回读清零

**Phase 6（粗纲/人物 HTTP）**：
- 扣费账本埋点：`api_call_logs` 写 project + durationMs
- 余额刷新接通，IPC 清零

**Phase 7（详纲 HTTP）**：
- `POST /api/generate/detailed-outline`，扣 5 积分
- 15 分钟超时，290s 实弹验证通过
- JSON 字段直接传对象，无需 stringify

**Phase 8（剧本 HTTP）**：
- V8 引擎迁移到 `server/src/application/script-generation/`
- HTTP 路由：Start/Pause/Resume/Stop/Rewrite
- 前端轮询驱动进度条，IPC 完全退场
- 实弹验证：2 集 54 秒，积分扣费正常

**旧山河粉碎令**：
- `src/main/` 从 150+ 文件缩到 7 文件
- `preload` 从 5 模块缩到 2
- renderer 20 文件 IPC→HTTP
- TypeScript 编译零错误

## 当前结论

1. HTTP 链路完全打通，旧 IPC 链路物理退场
2. Electron 桌面壳纯粹化：只管窗口/系统桥接/导出 markdown
3. 所有正式业务请求统一走 HTTP → server/ → PocketBase
4. 积分扣费正常，生成质量与原 Electron 链路一致

## 下一终端先看什么

1. 先读：`AGENTS.md` → `docs/聊天记录.md` → `docs/项目原则.md` → `docs/AI自我学习.md` → `docs/项目结构.md`
2. 部署详情：`docs/部署配置.md`
3. 新项目接入共享 PocketBase：`docs/部署配置.md` 第七、八节
4. 不要再把 `src/main/ipc` 和 `src/main/infrastructure/storage` 当正式主链

爱你，波别！