# 2026-04-14 主线

这份文档当前只服务这一条主线：

把 Stage 3 的“粗纲和人物读秒结束交白卷”问题收缩到真实根因，修住 `faction_matrix` 的超时、异常上浮和入口证据链，让前台不再假装成功等待。

## 当前主线在解决什么

这条主线解决的是“势力矩阵阶段已经超时或失败，但用户最后看到的是白板或笼统超时”的问题：

1. `faction_matrix` 之前没有独立任务身份，借 `rough_outline` 的壳跑。
2. agent 内部还写死了 `120000ms`，不适合 20 集世界观矩阵。
3. timeout 和 runtime 错误没有沿链路直白上浮到前台。
4. 入口缺少确认版七问的握手证据，排查时很难第一时间排除“空锅发车”。

## 为什么现在要做这条主线

- 用户真实跑 20 集粗纲和人物时，连续遇到：
  - 超时几百秒
  - 读秒结束交白卷
  - `faction_matrix_parse_failed`
  - `ai_request_timeout:120000ms`
- 最新证据已经坐实死点在：
  - `generateFactionMatrix(...)`
  - `generateCharacterProfilesFromConfirmedSevenQuestionsDefault(...)`
  - `generateOutlineAndCharactersFromConfirmedSevenQuestions(...)`
- 如果这层不收住，后面的粗纲 parser、人物 parser、前台 Stage 状态都会被误判成主因。

## 本轮任务清单

1. 给 `faction_matrix` 补独立任务身份和 timeout 预算。
2. timeout / runtime / parse 错误按势力矩阵对象名直接上浮，不再白板。
3. 在 Stage 3 入口补确认版七问握手日志。
4. 前台补齐势力矩阵相关红字翻译。
5. 跑最小回归和类型检查，准备真机复跑。

## 当前进度

- 当前任务：势力矩阵超时链修复
- 当前进度：`5/5`

## 完成定义

这条主线只有下面 5 件事同时成立才算完成：

1. `faction_matrix` 已有独立 `AiTaskKind` 和 `300000ms` 预算。
2. `faction-matrix-agent.ts` 超时会直接抛 `faction_matrix_timeout:*`。
3. `generate-outline-and-characters-from-confirmed-seven-questions.ts` 已记录七问握手证据，并且不吞 `faction_matrix` 异常。
4. 前台能把 `faction_matrix_timeout / generation_failed / parse_failed` 翻成明确中文。
5. `npm run typecheck`、`resolve-ai-stage-timeout.test.ts`、`faction-matrix-agent.test.ts` 已通过。
