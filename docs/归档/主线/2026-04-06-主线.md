# 2026-04-06 主线

这份文档对应上一条已经收口的主线，现已归档。

## 归档主线在解决什么

把剧本 production 正式收成 `agent-first` 主链，同时把“当前主线计划”收成仓库里的正式文档，避免继续无限往下做。

## 这条主线已完成的关键结果

- 第 1 刀：production 默认逐集 `Phase A`
- `Phase A = format_pollution -> scene_structure -> char_count`
- batch 后返修收成 `Phase B`
- `Phase B = continuity / character / theme`
- 共享 repair guard 提到 `screenplay-repair-guard.ts`
- 第 2 刀：首稿 prompt 开始瘦身
- 检测层补最小“问题 -> Agent”派工协议
- 四件套同步到当前事实
- 定向回归通过
- `npm run build` 通过
- 主线文档制度正式建立

## 这条主线的关键验证

### 真实 10 集 evidence

- 命令：`node tools/e2e/v11-wordcount-strap-test.mjs 10`
- 结果：`10/10` 全过
- 字数：`876-1709`
- 均值：`1245`
- 证据目录：`tools/e2e/out/userdata-v11-10ep-mnm0ssi8/`

## 为什么归档

因为这条主线最核心的验证项已经过了。

后面补出的 `20` 集问题，已经不是“这条主线继续补一点”的范畴，而是一个新的失败类型：

- `duplicate_scene_detected:同批新生成场景发生实质重复：scene_16_to_17`

所以按规则，不继续把旧主线无限续命，直接归档，切新主线。
