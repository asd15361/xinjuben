# 2026-04-13 主线

这份文档当前只服务这一条主线：

把已经完成的文档治理主线继续推进到目录层：收掉根目录脚本噪音，并把 `tools/e2e` 从混放状态整理成清楚的 runner / utils / tests 结构，让工程基座和验证工具链都不再靠人脑猜身份。

## 当前主线在解决什么

这条主线解决的是“现役入口虽然已经收住，但目录层还会继续制造误导”的问题：

1. 根目录原先大量 `tmp-* / check_* / fix_* / debug_* / find_*` 一次性脚本已经全部从根目录退场。
2. 这些历史排障脚本已经统一沉到 `scripts/archive/root-noise-2026-04-13/`。
3. `tools/e2e` 已完成物理分层：
   - `runners/official/`
   - `runners/observation/`
   - `utils/`
   - `tests/`
   - `utils/legacy/`
4. `package.json`、`runner-taxonomy` 和当前现役文档已同步到新路径口径。

## 为什么现在要做这条主线

- 用户后续明确要求先做“根目录脚本噪音身份审计与清除”。
- 如果根目录继续堆一次性脚本，哪怕文档入口再干净，下一终端还是会先被工程基座带偏。
- `tools/e2e` 如果继续把官方 runner、观察 runner、共用模块和旧辅助脚本混在一层，也会持续制造“这东西到底算不算正式口”的误判。

## 本轮任务清单

1. 根目录临时脚本完成消费方审计。
2. 无正式消费方的根目录噪音，已统一移出根目录。
3. `tools/e2e` 已完成 runner / utils / tests 物理分层。
4. `package.json`、`runner-taxonomy`、README 和四件套已同步更新。
5. 迁移后的官方口已做轻量验证。

## 当前进度

- 当前任务：根目录清剿与 e2e 分层
- 当前进度：`5/5`

## 完成定义

这条主线只有下面 5 件事同时成立才算完成：

1. 根目录临时脚本已经不再留在根目录。
2. `tools/e2e` 根目录已经从混放状态收成目录分层。
3. `package.json` 和 `tools/e2e/utils/runner-taxonomy.mjs` 已对齐新路径。
4. `npm run authority:check` 与 `node --test tools/e2e/tests/quality-gate.test.mjs` 已通过。
5. 四件套已经把这轮目录级收口同步进去。

