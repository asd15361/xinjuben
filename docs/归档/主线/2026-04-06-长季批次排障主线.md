# 2026-04-06 主线

这份文档只服务当前这一条主线。

做完就归档到 `docs/归档/主线/`，下一轮另起新的日期文档。

## 当前主线在解决什么

解决长季后半程批次失败问题。

这次不是内容质量大面积回退，而是：

- `20` 集真实跑到第 `16-20` 集批次时
- 命中了 `duplicate_scene_detected`
- 具体失败码：`duplicate_scene_detected:同批新生成场景发生实质重复：scene_16_to_17`

## 为什么现在要做这条主线

因为上一条主线已经完成了它的关键验证：

- `10` 集真实 evidence 已经 `10/10` 全过

现在真正新冒出来的主问题，不是 prompt 还要不要再瘦一点，而是：

1. 长季后半程批次里为什么会出现同批实质重复
2. 为什么 `16-20` 集都被标成 completed，但最终 `scriptStateLedger.sceneCount` 只有 `15`
3. 这个问题属于检测过严、落盘收口错误，还是生成承接本身真的重复

## 当前目标

把长季后半程这类失败收成一个明确结论：

- 是 `scene_16_to_17` 真重复
- 还是批次去重/落盘/收口逻辑误判
- 找到根因后再决定补哪一刀

## 当前任务清单

### 已完成

- [x] 真实 `20` 集 evidence 已跑
  - 命令：`node tools/e2e/v11-wordcount-strap-test.mjs 20`
  - 外层命令超时，但 runner 已返回最终统计
  - `generatedScenes=15, success=false`
  - 前 `15/15` 集全部过线
  - 字数：`863-1739`
  - 均值：`1325`
  - 证据目录：`tools/e2e/out/userdata-v11-20ep-mnm1macg/`
- [x] 失败码已抠出
  - `duplicate_scene_detected:同批新生成场景发生实质重复：scene_16_to_17`
- [x] projects.json 已确认
  - `batchIndex=4`
  - `startEpisode=16`
  - `endEpisode=20`
  - `batchContext.status=failed`
  - `scriptStateLedger.sceneCount=15`
- [x] 第 `16` 集 / 第 `17` 集 evidence 已对比
  - `ep16-evidence.json` 与 `ep17-evidence.json` 的正文基本同稿
  - 不是检测误判，而是模型把 `17` 集写成了 `16` 集近似重写
  - 上游粗纲 `16/17` 集摘要并不相同，所以当前更像是“剧本 prompt / 承接上下文压过了当前集差异”，不是 outline 天生重复
- [x] 最小修正已落
  - `build-scene-progression-directives.ts` 已补：
    - “这集不是上一集的重写稿”
    - 承接同一麻烦时，必须同时更换地点、争夺对象、主动作、结果中的至少两项
- [x] 定向验证通过
  - `create-script-generation-prompt.test.ts`
  - `build-scene-progression-directives.test.ts`
  - 共 `26/26` 通过
  - `npm run typecheck:node` 通过

### 未完成

- [ ] 用当前修正重新跑真实 `20` 集 evidence
- [ ] 判断这次最小修正是否足以消掉 `scene_16_to_17` 重复
- [ ] 如果仍失败，再继续查落盘链 / duplicate detector / batch 收口

## 当前进度

- 当前状态：失败码、重复证据和最小修正都已完成
- 下一关键动作：带着新 prompt 合同复跑真实 `20` 集

## 完成定义

这条主线只有在下面两件事都成立时才算完成：

1. `scene_16_to_17` 的重复根因已经明确
2. 已经能清楚决定下一刀是修检测、修落盘，还是修生成链

## 明确不做什么

- 不把这个新问题继续混写成上一条 `agent-first` 主线
- 不在还没确认重复根因前，先去乱补 prompt
- 不把“前 15 集都过线”误写成“20 集已经稳了”

## 本轮新增结论（2026-04-06 深夜）

- 已补第二刀 prompt 收口：
  - `build-scene-progression-directives.ts` 不再把上一集整段剧本文字直接塞回承接块，现在只保留压缩摘要
  - `create-script-generation-prompt.ts` 已把 `当前集任务` 和 `当集最后一场结果落点` 提前到 prompt 前部
- 定向验证：
  - `node --test src/main/application/script-generation/prompt/build-scene-progression-directives.test.ts src/main/application/script-generation/prompt/create-script-generation-prompt.test.ts`
  - `28/28` 通过
  - `npm run typecheck:node` 通过
- 真实 `20` 集已复跑：
  - 命令：`node tools/e2e/v11-wordcount-strap-test.mjs 20`
  - 新证据目录：
    - `tools/e2e/out/userdata-v11-20ep-mnm5kyoh/`
    - `tools/e2e/out/evidence-v11-20ep-mnm5kyoh/`
  - 外层命令再次超时，但 runner 已落盘结果
- 新失败结论：
  - 失败点前移到 `duplicate_scene_detected:同批新生成场景发生实质重复：scene_11_to_12`
  - `ep11-evidence.json` 与 `ep12-evidence.json` 基本同稿
  - 当前批次只生成到第 `10` 集，`scriptStateLedger.sceneCount=10`
- 当前根因判断进一步收缩：
  - “上一集整段全文被塞回 prompt 导致复写”这条根因已被部分打中，这刀有效
  - 但还不是最后根因；`ep11 promptLength=4149`，`ep12 promptLength=12281`
  - 更像是批内第二集的 `ledger / momentum / recap` 里仍有正文级回灌

## 下一刀

1. 查 `ep12` prompt 为什么又暴涨到 `12281`
2. 重点看：
   - `buildLedgerAssertionBlock`
   - `buildKnowledgeBoundaryBlock`
   - `buildMomentumBridgeBlock`
   - `buildCompactLedgerBridgeBlock`
3. 继续把正文级 recap 收成摘要级 recap
4. 再跑真实 `20` 集，看失败点是否继续前移或直接消失

## 本轮再次新增结论（2026-04-06 清晨）

- [x] 已确认一个之前的误判
  - `evidence.promptLength` 记录的是**当次尝试实际发送的 prompt**
  - 它不一定等于 base prompt；如果 episode 进入第 2/3 次尝试，会被 `buildEpisodeEditPrompt()` 或 `char-count-agent` prompt 放大
  - 所以不能再把某集 `promptLength` 暴涨，直接等同于 base prompt recap 暴涨
- [x] 已补 attempt 级 evidence 落盘
  - `run-script-generation-batch.ts` 现在会把每次尝试的 metadata 一起写 evidence
  - 已包含：`attempt`、`task`、`failures`
  - 目的：下次可直接区分“首发 prompt 太大”还是“二次改稿 prompt 太大”
- [x] 最新真实 `20` 集复跑出现更大的新事实
  - 新证据目录：
    - `tools/e2e/out/userdata-v11-20ep-mnmaylv4/`
    - `tools/e2e/out/evidence-v11-20ep-mnmaylv4/`
  - evidence 已生成到 `ep20-evidence.json`
  - `ep17-evidence.json` 当前不再是上一集复写，且 `promptLength=1756`
  - 这说明这轮并**没有再次复现 `16->17` 重复**
- [x] 当前主问题已切换
  - 尽管 evidence 已跑到 `ep20`
  - 但 `workspace/projects.json` 仍停在：
    - `stage = detailed_outline`
    - `scriptDraft = []`
    - `visibleResult.status = pending`
    - `generationStatus = script / Generation in progress`
  - 当前更像是：
    - 剧本 batch 实际已经跑完或基本跑完
    - 但保存链 / 返回链 / 前台状态更新没有收口

## 当前下一刀

1. 不再继续把“duplicate_scene_detected”当唯一主问题
2. 转查“evidence 已到 ep20，但项目仍未保存”的收口链
3. 重点看：
   - `start-script-generation.ts`
   - `script-generation-runtime-handlers.ts`
   - `finalize-script-postflight.ts`
   - 项目持久化与 `visibleResult / generationStatus` 写回链
