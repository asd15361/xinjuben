# 项目打开卡顿验收驳回与收口修复计划（2026-03-24）

## TL;DR

> **Summary**: 当前“已完成”判断无效。本计划用于修复验收驳回中确认的权威分裂、stale data、弱指纹、render 热路径与静态门禁缺口，直到重新达到可接受交付状态。
> **Deliverables**:
>
> - 修复 `workspace-project-handlers.ts` 的重复注册与错误快照入口
> - 将 stage 切换统一收口到 `stage-session-service`
> - 恢复 `activeCharacterBlocks` 单一正式生产者并消除 stale save path
> - 重做 script plan fingerprint 与 script quality audit 路径
> - 补齐 lint 范围修复、定向测试、grep 门禁与性能复验
>   **Effort**: Medium
>   **Parallel**: YES - 4 waves
>   **Critical Path**: 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8

## Context

### Original Request

把刚才的分析做成计划。

### Interview Summary

- 本轮不是继续争论“是不是基本做完”，而是把已确认的验收驳回发现转成正式执行计划。
- 用户提供的分析已确认：重复 IPC 注册、项目打开全量快照旁路、切页未收口、`activeCharacterBlocks` 缺正式生产者、home/session 双写、script plan 指纹过弱、script quality audit 仍在 render 路径、lint 红、定向测试缺失。
- 这轮必须先修“权威与真相”，再谈性能通过；不能把性能调优和架构收口混成同一步。

### Metis Review (gaps addressed)

- 补充了明确顺序：**权威收口 → stale-data elimination → render-path removal → static hygiene → performance revalidation**。
- 补充了 race/stale overwrite/重复注册/legacy call-site 的强制验收门禁。
- 补充了范围约束：不扩成通用 IPC 框架重写，不扩成全局缓存重构，不引入 E2E/Playwright。

## Work Objectives

### Core Objective

把“项目打开卡顿根因治理”从错误的“已完成”状态拉回到真实可交付状态：消除当前已确认的 authority conflicts、stale data paths、错误缓存失效和 render 热路径，让重新验收建立在单一真相与可复核证据之上。

### Deliverables

- `src/main/ipc/workspace-project-handlers.ts` 重复 handler 清零，项目打开入口改为正式会话/快照边界
- `src/renderer/src/app/services/stage-session-service.ts` 成为 renderer 唯一 stage session authority
- renderer 所有 stage 切换调用方不再直连 `window.api.workspace.changeProjectStage`
- `activeCharacterBlocks` 恢复 authoritative producer，并在 `useStageStore.ts` 中通过正式路径 hydrate/save
- `src/renderer/src/app/services/script-plan-service.ts` 改为正确失效语义
- `src/renderer/src/features/script/ui/useScriptSceneIndex.ts` / `ScriptSceneList.tsx` 退出 render-critical 质量审计路径
- lint scope truthful 化，目标定向测试补齐，grep 门禁补齐
- 最终重新完成 typecheck / lint / build / file-scoped tests / grep / 性能复验

### Definition of Done (verifiable conditions with commands)

- `npm run typecheck` 通过
- `npm run lint` 通过，且不是靠人工忽略临时目录
- `npm run build` 通过
- `node --test src/renderer/src/features/character/model/derive-active-character-blocks.test.ts` 通过
- `node --test src/renderer/src/features/detailed-outline/model/build-detailed-outline-view-model.test.ts` 通过
- `node --test src/renderer/src/features/script/model/build-script-scene-summary-index.test.ts` 通过
- `node --test src/renderer/src/features/script/model/script-scene-quality-audit.test.ts` 通过
- `node --test src/renderer/src/app/services/script-plan-service.test.ts` 通过
- `rg -n "ipcMain\.handle\('workspace:open-project-session'" src/main/ipc/workspace-project-handlers.ts` 结果为 1
- `rg -n "ipcMain\.handle\('workspace:switch-stage-session'" src/main/ipc/workspace-project-handlers.ts` 结果为 1
- `rg -n "window\.api\.workspace\.changeProjectStage\(" src/renderer/src` 结果只允许剩单一 service/compat 文件，且计划内明确指定
- `rg -n "getProjectSnapshot\(projectId, undefined\)" src/main/ipc/workspace-project-handlers.ts` 结果为 0
- `rg -n "setProjectName\(payload\.projectId\)|setProjectNameInShell\(result\.payload\.projectId\)" src/renderer/src` 结果为 0
- `rg -n "inspectScreenplayQualityEpisode" src/renderer/src/features/script` 结果只允许留在按需 audit 文件

### Must Have

- 单一 stage session authority
- 单一 `activeCharacterBlocks` producer
- truth-first fingerprint invalidation
- script quality audit 退出 render 主链
- lint / tests / grep 全部过线后才重开性能验收

### Must NOT Have (guardrails, AI slop patterns, scope boundaries)

- 不得把“看起来快一点”当验收通过
- 不得保留 renderer 多点直连 `changeProjectStage`
- 不得继续用 `getProjectSnapshot(projectId, undefined)` 作为项目打开主链
- 不得只删 CharacterStage effect 而不补 authoritative producer
- 不得把 lint 红线解释成“临时目录无所谓”
- 不得扩大到无关 store 架构重写、无关 IPC 平台重做、无关 UI 改版
- 不得写入 E2E、Playwright、浏览器自动化验收、长链路 smoke

## Verification Strategy

> ZERO HUMAN INTERVENTION — all verification is agent-executed.

- Test decision: tests-after + Node file-scoped tests
- QA policy: Every task has agent-executed scenarios
- Evidence: `.sisyphus/evidence/task-{N}-{slug}.{ext}`

## Execution Strategy

### Parallel Execution Waves

> Target: 5-8 tasks per wave. <3 per wave (except final) = under-splitting.
> Extract shared dependencies as Wave-1 tasks for max parallelism.

Wave 1: authority baseline and static guardrails (tasks 1-3)
Wave 2: stale-data and ownership repair (tasks 4-6)
Wave 3: render-path and invalidation repair (tasks 7-8)
Wave 4: tests, lint, grep, performance revalidation, docs evidence (tasks 9-10)

### Dependency Matrix (full, all tasks)

- 1 blocks 2, 3, 4, 5, 6, 7, 8, 9, 10
- 2 blocks 5, 6, 9, 10
- 3 blocks 4, 5, 6, 9, 10
- 4 blocks 5, 7, 9, 10
- 5 blocks 6, 9, 10
- 6 blocks 9, 10
- 7 blocks 8, 9, 10
- 8 blocks 9, 10
- 9 blocks 10
- 10 blocks Final Verification Wave

### Agent Dispatch Summary (wave → task count → categories)

- Wave 1 → 3 tasks → `deep`, `unspecified-high`
- Wave 2 → 3 tasks → `deep`, `quick`
- Wave 3 → 2 tasks → `unspecified-high`, `quick`
- Wave 4 → 2 tasks → `quick`, `deep`

## TODOs

> Implementation + Test = ONE task. Never separate.
> EVERY task MUST have: Agent Profile + Parallelization + QA Scenarios.

- [ ] 1. Remove duplicate workspace IPC registrations and freeze single authority bootstrap

  **What to do**: In `src/main/ipc/workspace-project-handlers.ts`, remove the second registrations of `workspace:get-project`, `workspace:open-project-session`, and `workspace:switch-stage-session`. Keep one canonical registration site for each, preserve existing DTO behavior, and add targeted tests or static assertions proving duplicate registration cannot silently reappear during supported bootstrap. If the file currently interleaves old and new handlers, collapse the authoritative definitions into one contiguous section so future edits cannot accidentally re-add duplicates lower in the file.
  **Must NOT do**: Do not redesign unrelated IPC channels, do not rename the channel contracts, and do not batch unrelated cleanup into this task.

  **Recommended Agent Profile**:
  - Category: `deep` — Reason: authority repair in Electron IPC bootstrap has high blast radius and requires careful sequencing.
  - Skills: [`test-driven-development`] — write failing checks for single registration before repair.
  - Omitted: [`webapp-testing`] — no browser/E2E needed for this repo-scoped authority task.

  **Parallelization**: Can Parallel: NO | Wave 1 | Blocks: 2, 3, 4, 5, 6, 7, 8, 9, 10 | Blocked By: none

  **References** (executor has NO interview context — be exhaustive):
  - Pattern: `src/main/ipc/workspace-project-handlers.ts:187-228` — first registration block currently contains `get-project`, `open-project-session`, `switch-stage-session`.
  - Pattern: `src/main/ipc/workspace-project-handlers.ts:230-368` — duplicate registration block that must be removed.
  - API/Type: `src/shared/contracts/workspace.ts` — keep existing workspace IPC DTO contracts stable while deduplicating handlers.
  - Pattern: `.sisyphus/plans/项目打开卡顿验收驳回与收口修复计划（2026-03-24）.md:Definition of Done` — exact grep gate for handler count.

  **Acceptance Criteria** (agent-executable only):
  - [ ] `rg -n "ipcMain\.handle\('workspace:open-project-session'" src/main/ipc/workspace-project-handlers.ts` returns exactly 1 match.
  - [ ] `rg -n "ipcMain\.handle\('workspace:switch-stage-session'" src/main/ipc/workspace-project-handlers.ts` returns exactly 1 match.
  - [ ] `rg -n "ipcMain\.handle\('workspace:get-project'" src/main/ipc/workspace-project-handlers.ts` returns exactly 1 match.
  - [ ] `npm run typecheck` passes after deduplication.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Duplicate handler static audit
    Tool: Bash
    Steps: Run the three rg commands above against src/main/ipc/workspace-project-handlers.ts.
    Expected: Each command reports exactly one live registration site.
    Evidence: .sisyphus/evidence/task-1-ipc-registration-audit.txt

  Scenario: Regression compile after authority cleanup
    Tool: Bash
    Steps: Run `npm run typecheck` after removing duplicate registrations.
    Expected: TypeScript passes; no missing symbol or duplicate-registration-related compile break appears.
    Evidence: .sisyphus/evidence/task-1-ipc-registration-typecheck.txt
  ```

  **Commit**: YES | Message: `fix(ipc): dedupe workspace session handlers` | Files: [`src/main/ipc/workspace-project-handlers.ts`, `tests or assertions added for handler uniqueness`]

- [ ] 2. Route project-open through authoritative stage-session snapshot contract

  **What to do**: Update `workspace:open-project-session` in `src/main/ipc/workspace-project-handlers.ts` so it no longer calls `getProjectSnapshot(projectId, undefined)`. The project-open path must load only the authoritative data required for the chosen session/stage contract, preserving a single session semantics instead of whole-project bypass loading. Keep `switch-stage-session` aligned with the same authority model so open and switch are contract siblings, not two different loading philosophies.
  **Must NOT do**: Do not preserve `undefined = load all stages` as a hidden fallback, and do not reintroduce split loading through `openProjectShell` / `getStagePayload` in renderer.

  **Recommended Agent Profile**:
  - Category: `deep` — Reason: this changes the session authority boundary and affects project-open performance truth.
  - Skills: [`test-driven-development`] — lock failing behavior first with a targeted regression.
  - Omitted: [`backend-patterns`] — not needed beyond the focused Electron/main-session contract.

  **Parallelization**: Can Parallel: NO | Wave 1 | Blocks: 5, 6, 9, 10 | Blocked By: 1

  **References** (executor has NO interview context — be exhaustive):
  - Pattern: `src/main/ipc/workspace-project-handlers.ts:195-207` — current `open-project-session` implementation still uses `getProjectSnapshot(projectId, undefined)`.
  - Pattern: `src/main/ipc/workspace-project-handlers.ts:213-228` — `switch-stage-session` already loads a targeted stage snapshot and should remain the comparison baseline.
  - API/Type: `src/shared/contracts/project.ts` — `ProjectSnapshotDto['stage']` drives the next-stage contract.
  - API/Type: `src/shared/contracts/workspace.ts` — `StagePayloadDto` and input DTOs must remain compatible with preload/renderer consumers.
  - Pattern: `src/preload/api/workspace.ts` — ensure renderer API shape still matches the adjusted open-session contract.

  **Acceptance Criteria** (agent-executable only):
  - [ ] `rg -n "getProjectSnapshot\(projectId, undefined\)" src/main/ipc/workspace-project-handlers.ts` returns 0 matches.
  - [ ] `npm run typecheck` passes.
  - [ ] `npm run build` passes.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Open-session bypass path removed
    Tool: Bash
    Steps: Run `rg -n "getProjectSnapshot\(projectId, undefined\)" src/main/ipc/workspace-project-handlers.ts`.
    Expected: No match remains in the file.
    Evidence: .sisyphus/evidence/task-2-open-session-no-full-snapshot.txt

  Scenario: Main-process contract still compiles
    Tool: Bash
    Steps: Run `npm run typecheck && npm run build` after the open-session contract refactor.
    Expected: Both commands pass, proving main/preload/shared contract consistency.
    Evidence: .sisyphus/evidence/task-2-open-session-build.txt
  ```

  **Commit**: YES | Message: `fix(workspace): remove full snapshot open-session bypass` | Files: [`src/main/ipc/workspace-project-handlers.ts`, `related contract/preload files if touched`]

- [ ] 3. Migrate all renderer stage switches to stage-session-service and retire direct changeProjectStage authority

  **What to do**: Replace renderer direct calls to `window.api.workspace.changeProjectStage()` with `stage-session-service` routing. Required callers include `AppSidebar.tsx`, `AppHeader.tsx`, `CharacterStage.tsx`, `DetailedOutlineStage.tsx`, `ScriptStage.tsx`, `OutlineStage.tsx`, `ChatStage.tsx`, `useChatStageActions.ts`, and `ProjectGenerationBanner.tsx` unless a single approved compat shim remains and is explicitly documented. After migration, stage mutation ownership must live in `src/renderer/src/app/services/stage-session-service.ts` only.
  **Must NOT do**: Do not leave mixed authority where some callers still mutate stage directly “for convenience,” and do not keep duplicate hydration logic in call sites.

  **Recommended Agent Profile**:
  - Category: `unspecified-high` — Reason: many call sites across renderer, but the change is structurally repetitive once authority is chosen.
  - Skills: [`test-driven-development`] — pin failing grep/static checks before call-site migration.
  - Omitted: [`frontend-patterns`] — this is control-path consolidation, not UI architecture work.

  **Parallelization**: Can Parallel: YES | Wave 1 | Blocks: 4, 5, 6, 9, 10 | Blocked By: 1

  **References** (executor has NO interview context — be exhaustive):
  - Pattern: `src/renderer/src/app/services/stage-session-service.ts:33-87` — canonical `openProjectSession()` and `switchStageSession()` service API.
  - Pattern: `src/renderer/src/app/shell/AppSidebar.tsx:33` — confirmed direct `changeProjectStage` call.
  - Pattern: `src/renderer/src/app/shell/AppHeader.tsx:60` — confirmed direct `changeProjectStage` call.
  - Pattern: `src/renderer/src/features/character/ui/CharacterStage.tsx:59-70` — confirmed direct stage change after save.
  - Pattern: `src/renderer/src/features/detailed-outline/ui/DetailedOutlineStage.tsx` — multiple direct calls remain.
  - Pattern: `src/renderer/src/features/script/ui/ScriptStage.tsx:86` — direct stage change remains.
  - Pattern: `src/renderer/src/components/ProjectGenerationBanner.tsx:138` — direct stage change remains.
  - Pattern: `.sisyphus/plans/项目打开卡顿验收驳回与收口修复计划（2026-03-24）.md:Definition of Done` — grep gate allows only one approved remaining entry if intentionally kept.

  **Acceptance Criteria** (agent-executable only):
  - [ ] `rg -n "window\.api\.workspace\.changeProjectStage\(" src/renderer/src` returns only the approved service/compat location documented in the implementation.
  - [ ] `npm run typecheck` passes after call-site migration.
  - [ ] Stage switch callers no longer duplicate `hydrateStagePayload` / `applyStageTransition` locally.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Renderer authority grep audit
    Tool: Bash
    Steps: Run `rg -n "window\.api\.workspace\.changeProjectStage\(" src/renderer/src`.
    Expected: Direct renderer call sites are removed; only the approved centralized path remains.
    Evidence: .sisyphus/evidence/task-3-change-project-stage-grep.txt

  Scenario: Renderer compile after stage routing migration
    Tool: Bash
    Steps: Run `npm run typecheck` after replacing call sites with stage-session-service.
    Expected: TypeScript passes and migrated callers resolve to the service correctly.
    Evidence: .sisyphus/evidence/task-3-stage-routing-typecheck.txt
  ```

  **Commit**: YES | Message: `refactor(renderer): centralize stage session switching` | Files: [`src/renderer/src/app/services/stage-session-service.ts`, `src/renderer/src/app/shell/AppSidebar.tsx`, `src/renderer/src/app/shell/AppHeader.tsx`, `src/renderer/src/features/**`, `src/renderer/src/components/ProjectGenerationBanner.tsx`]

- [ ] 4. Restore a single authoritative producer for activeCharacterBlocks and remove stale save semantics

  **What to do**: Define and wire one authoritative producer for `activeCharacterBlocks` so Character, Detailed Outline, and Script flows all consume truthful derived state. Update `CharacterStage.tsx`, `useStageStore.ts`, and any shared builder path so `activeCharacterBlocks` is recomputed or hydrated through one official source, then persisted through a truthful save path. Remove the current behavior where CharacterStage no longer writes back but `saveCharacterDrafts()` still persists a potentially stale store snapshot.
  **Must NOT do**: Do not restore the old render/effect writeback loop, and do not leave `saveCharacterDrafts()` consuming stale `activeCharacterBlocks` from unrelated prior hydration.

  **Recommended Agent Profile**:
  - Category: `deep` — Reason: this repairs data truth shared across character, detailed-outline, script, and plan-building paths.
  - Skills: [`test-driven-development`] — add failing derivation/save tests first.
  - Omitted: [`frontend-patterns`] — data authority is the main concern here.

  **Parallelization**: Can Parallel: YES | Wave 2 | Blocks: 5, 7, 9, 10 | Blocked By: 3

  **References** (executor has NO interview context — be exhaustive):
  - Pattern: `src/renderer/src/features/character/ui/CharacterStage.tsx:49-57` — current page computes `availableBlocks` only.
  - Pattern: `src/renderer/src/store/useStageStore.ts:344-355` — script hydration still reads `payload.activeCharacterBlocks`.
  - Pattern: `src/renderer/src/store/useStageStore.ts:434-455` — `replaceActiveCharacterBlocks()` exists and `saveCharacterDrafts()` still persists store-held `activeCharacterBlocks`.
  - Pattern: `src/shared/domain/workflow/planning-blocks.ts` — likely shared builder authority for block derivation.
  - Test target: `src/renderer/src/features/character/model/derive-active-character-blocks.test.ts` — required by plan verification.

  **Acceptance Criteria** (agent-executable only):
  - [ ] `node --test src/renderer/src/features/character/model/derive-active-character-blocks.test.ts` passes.
  - [ ] `saveCharacterDrafts()` no longer persists a stale `activeCharacterBlocks` snapshot when character inputs change.
  - [ ] `npm run typecheck` passes.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Derived activeCharacterBlocks remains truthful after character edits
    Tool: Bash
    Steps: Run `node --test src/renderer/src/features/character/model/derive-active-character-blocks.test.ts`.
    Expected: Tests prove authoritative derivation updates when relevant character/outline inputs change and does not require render-time writeback.
    Evidence: .sisyphus/evidence/task-4-active-character-blocks-test.txt

  Scenario: Store save path compile audit
    Tool: Bash
    Steps: Run `npm run typecheck` after updating producer/save logic.
    Expected: TypeScript passes; no consumer remains dependent on the removed stale path.
    Evidence: .sisyphus/evidence/task-4-active-character-blocks-typecheck.txt
  ```

  **Commit**: YES | Message: `fix(character): restore authoritative active block derivation` | Files: [`src/renderer/src/features/character/ui/CharacterStage.tsx`, `src/renderer/src/store/useStageStore.ts`, `src/shared/domain/workflow/planning-blocks.ts`, `src/renderer/src/features/character/model/derive-active-character-blocks.test.ts`]

- [ ] 5. Remove duplicate project-name and stage-transition writers between home actions and stage-session-service

  **What to do**: Make `stage-session-service` the single owner for project/session hydration side effects. Remove duplicated `setProjectId`, `setProjectName`, `clearGenerationNotice`, and `applyStageTransition` logic from `useHomePageActions.ts` where it repeats what `stage-session-service` already does. Correct the bug where project name is incorrectly set from `payload.projectId` instead of the real project name. Ensure project creation entry continues to work with one clear writer path.
  **Must NOT do**: Do not keep a “temporary duplicate” owner, and do not leave `projectName` set from `projectId` anywhere in renderer.

  **Recommended Agent Profile**:
  - Category: `quick` — Reason: ownership decision is already made; the implementation is a focused cleanup with regression risk.
  - Skills: [`test-driven-development`] — add minimal regression checks before removing duplicate writers.
  - Omitted: [`backend-patterns`] — no backend work here.

  **Parallelization**: Can Parallel: YES | Wave 2 | Blocks: 6, 9, 10 | Blocked By: 2, 3

  **References** (executor has NO interview context — be exhaustive):
  - Pattern: `src/renderer/src/app/services/stage-session-service.ts:125-139` — current hydration path writes `projectId`, `projectName`, and applies stage transition.
  - Pattern: `src/renderer/src/app/services/stage-session-service.ts:135` — current bug sets project name from `payload.projectId`.
  - Pattern: `src/renderer/src/features/home/ui/useHomePageActions.ts:109-118` — `enterProject()` duplicates project/session writers.
  - Pattern: `src/renderer/src/features/home/ui/useHomePageActions.ts:142-154` — `openProject()` duplicates writers again and sets projectName from `result.payload.projectId`.

  **Acceptance Criteria** (agent-executable only):
  - [ ] `rg -n "setProjectName\(payload\.projectId\)|setProjectNameInShell\(result\.payload\.projectId\)" src/renderer/src` returns 0 matches.
  - [ ] `npm run typecheck` passes.
  - [ ] Project-open path has one ownership site for `applyStageTransition` in the implemented authority flow.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Project name bug grep audit
    Tool: Bash
    Steps: Run `rg -n "setProjectName\(payload\.projectId\)|setProjectNameInShell\(result\.payload\.projectId\)" src/renderer/src`.
    Expected: No match remains; project name is no longer sourced from projectId.
    Evidence: .sisyphus/evidence/task-5-project-name-grep.txt

  Scenario: Home/session ownership compile audit
    Tool: Bash
    Steps: Run `npm run typecheck` after removing duplicate writers from home actions.
    Expected: TypeScript passes; home page continues to compile against the centralized session service path.
    Evidence: .sisyphus/evidence/task-5-home-session-typecheck.txt
  ```

  **Commit**: YES | Message: `fix(home): remove duplicate session state writers` | Files: [`src/renderer/src/app/services/stage-session-service.ts`, `src/renderer/src/features/home/ui/useHomePageActions.ts`]

- [ ] 6. Add stale-response suppression and race-safe session ownership for open and switch flows

  **What to do**: Harden `stage-session-service` and its callers so late open/switch results cannot overwrite a newer requested project or stage. Introduce request identity, last-request-wins semantics, or equivalent authority gating for project-open and stage-switch operations. Cover rapid open A → open B, repeated stage switch spam, and inflight switch results landing after a newer stage request.
  **Must NOT do**: Do not rely on timing luck, and do not leave stale asynchronous results able to hydrate stores after a newer authoritative request is already in progress or completed.

  **Recommended Agent Profile**:
  - Category: `deep` — Reason: race-condition hardening across async session and store hydration boundaries.
  - Skills: [`test-driven-development`] — write failing race-focused tests first.
  - Omitted: [`webapp-testing`] — repo-local async tests are sufficient.

  **Parallelization**: Can Parallel: YES | Wave 2 | Blocks: 9, 10 | Blocked By: 2, 3, 5

  **References** (executor has NO interview context — be exhaustive):
  - Pattern: `src/renderer/src/app/services/stage-session-service.ts:33-87` — current open/switch async service API.
  - Pattern: `src/renderer/src/app/services/stage-session-service.ts:93-140` — hydration and transition side-effect path that must reject stale results.
  - Pattern: `src/renderer/src/features/home/ui/useHomePageActions.ts:142-156` — project-open caller path.
  - Metis guardrail: race/stale overwrite suppression must be explicitly tested before reopening performance claims.

  **Acceptance Criteria** (agent-executable only):
  - [ ] A targeted test proves stale open/switch results cannot overwrite newer requests.
  - [ ] `npm run typecheck` passes.
  - [ ] The implementation documents and enforces last-request-wins semantics in the service layer.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Open/switch stale-response suppression test
    Tool: Bash
    Steps: Run the targeted Node test file added for session race handling.
    Expected: The test proves a late response from an older request does not hydrate over a newer authoritative request.
    Evidence: .sisyphus/evidence/task-6-session-race-test.txt

  Scenario: Service compile after race hardening
    Tool: Bash
    Steps: Run `npm run typecheck`.
    Expected: TypeScript passes; request identity or equivalent gating compiles cleanly across service callers.
    Evidence: .sisyphus/evidence/task-6-session-race-typecheck.txt
  ```

  **Commit**: YES | Message: `fix(session): reject stale open and switch results` | Files: [`src/renderer/src/app/services/stage-session-service.ts`, `related session-race test files`, `affected callers if needed`]

- [ ] 7. Strengthen script-plan fingerprint invalidation to prefer correctness over cache hit rate

  **What to do**: Rework `src/renderer/src/app/services/script-plan-service.ts` so cache invalidation tracks all materially relevant plan inputs, not just counts. At minimum include truthful invalidation for `activeCharacterBlocks`, `detailedOutlineBlocks`, script content changes, and any upstream plan-driving outline/character changes that can alter output while lengths remain constant. If the current store lacks explicit revision fields, add lightweight revision/fingerprint inputs in the narrowest scope required for truthful invalidation. Cover both change and no-change cases with tests.
  **Must NOT do**: Do not keep the current length-only fingerprint, do not optimize for hit rate at the expense of correctness, and do not broaden this into a global cache framework rewrite.

  **Recommended Agent Profile**:
  - Category: `unspecified-high` — Reason: cache semantics affect correctness across multiple consumers and require careful invalidation boundaries.
  - Skills: [`test-driven-development`] — failing cache invalidation tests first.
  - Omitted: [`backend-patterns`] — this is a focused renderer/service invalidation problem.

  **Parallelization**: Can Parallel: YES | Wave 3 | Blocks: 8, 9, 10 | Blocked By: 4

  **References** (executor has NO interview context — be exhaustive):
  - Pattern: `src/renderer/src/app/services/script-plan-service.ts:15-31` — current `computeRevision` logic only considers mode, targetEpisodes, and a few lengths.
  - Pattern: `src/renderer/src/app/services/script-plan-service.ts:33-77` — current cache consumer path using the weak revision.
  - Pattern: `src/renderer/src/store/useStageStore.ts:344-351` — script hydration stores `outline`, `characters`, `activeCharacterBlocks`, `detailedOutlineBlocks` as upstream plan inputs.
  - Pattern: `src/renderer/src/app/services/script-plan-service.ts:52` — single renderer entry to `buildScriptGenerationPlan` that must remain authoritative.
  - Test target: `src/renderer/src/app/services/script-plan-service.test.ts` — required by plan verification.

  **Acceptance Criteria** (agent-executable only):
  - [ ] `node --test src/renderer/src/app/services/script-plan-service.test.ts` passes.
  - [ ] Tests prove plan cache invalidates when upstream semantic inputs change without length changes.
  - [ ] Tests prove plan cache does not invalidate on unrelated no-op inputs.
  - [ ] `npm run typecheck` passes.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Script-plan fingerprint change/no-change regression
    Tool: Bash
    Steps: Run `node --test src/renderer/src/app/services/script-plan-service.test.ts`.
    Expected: Tests prove semantic changes invalidate the cache and true no-op inputs do not.
    Evidence: .sisyphus/evidence/task-7-script-plan-service-test.txt

  Scenario: Compile after fingerprint hardening
    Tool: Bash
    Steps: Run `npm run typecheck` after updating script-plan fingerprint logic.
    Expected: TypeScript passes; all service consumers remain compatible.
    Evidence: .sisyphus/evidence/task-7-script-plan-typecheck.txt
  ```

  **Commit**: YES | Message: `fix(script-plan): make cache invalidation truthful` | Files: [`src/renderer/src/app/services/script-plan-service.ts`, `src/renderer/src/app/services/script-plan-service.test.ts`, `any minimal supporting revision files`]

- [ ] 8. Remove script quality audit and full screenplay construction from the list render-critical path

  **What to do**: Split light summary indexing from quality audit execution. Refactor `useScriptSceneIndex.ts` and `ScriptSceneList.tsx` so the list path only consumes precomputed lightweight summary data, and quality audit runs only through an explicit on-demand audit module/service. Remove `useScriptSceneQuality()` from `SceneCard` render flow and stop building full screenplay text for every visible card unless the card is explicitly expanded and the design requires that exact content. If needed, introduce the planned files `build-script-scene-summary-index.ts` and `script-scene-quality-audit.ts` to separate the concerns clearly.
  **Must NOT do**: Do not leave `inspectScreenplayQualityEpisode()` reachable from the list render path, and do not keep `buildScreenplayFromStructuredScene()` executing for every visible card when collapsed state already has a summary.

  **Recommended Agent Profile**:
  - Category: `unspecified-high` — Reason: this is both correctness of architecture boundary and performance hot-path reduction in script UI.
  - Skills: [`test-driven-development`] — add summary/audit separation tests first.
  - Omitted: [`frontend-patterns`] — visual design is not the task; render-path correctness is.

  **Parallelization**: Can Parallel: YES | Wave 3 | Blocks: 9, 10 | Blocked By: 4, 7

  **References** (executor has NO interview context — be exhaustive):
  - Pattern: `src/renderer/src/features/script/ui/useScriptSceneIndex.ts:52-79` — summary builder and `inspectSceneQuality()` still live in the same module; quality remains coupled.
  - Pattern: `src/renderer/src/features/script/ui/useScriptSceneIndex.ts:86-93` — `useScriptSceneQuality()` still invokes audit by scene in UI land.
  - Pattern: `src/renderer/src/features/script/ui/ScriptSceneList.tsx:97-109` — `SceneCard` currently runs `useScriptSceneQuality(script, scene.sceneNo)` and `buildScreenplayFromStructuredScene(seg)`.
  - Test targets: `src/renderer/src/features/script/model/build-script-scene-summary-index.test.ts`, `src/renderer/src/features/script/model/script-scene-quality-audit.test.ts` — required by plan verification.
  - Planned new files from earlier acceptance criteria: `src/renderer/src/features/script/model/build-script-scene-summary-index.ts`, `src/renderer/src/features/script/model/script-scene-quality-audit.ts`.

  **Acceptance Criteria** (agent-executable only):
  - [ ] `node --test src/renderer/src/features/script/model/build-script-scene-summary-index.test.ts` passes.
  - [ ] `node --test src/renderer/src/features/script/model/script-scene-quality-audit.test.ts` passes.
  - [ ] `rg -n "inspectScreenplayQualityEpisode" src/renderer/src/features/script` returns matches only in the explicit audit module/test files.
  - [ ] `npm run typecheck` passes.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Summary and audit separation tests
    Tool: Bash
    Steps: Run `node --test src/renderer/src/features/script/model/build-script-scene-summary-index.test.ts && node --test src/renderer/src/features/script/model/script-scene-quality-audit.test.ts`.
    Expected: Summary indexing and quality audit are independently verified; list path no longer depends on the audit path.
    Evidence: .sisyphus/evidence/task-8-script-summary-audit-tests.txt

  Scenario: Script audit grep audit
    Tool: Bash
    Steps: Run `rg -n "inspectScreenplayQualityEpisode" src/renderer/src/features/script`.
    Expected: Matches remain only in the explicit on-demand audit module and related tests, not in list/render files.
    Evidence: .sisyphus/evidence/task-8-script-audit-grep.txt
  ```

  **Commit**: YES | Message: `refactor(script): move quality audit off list render path` | Files: [`src/renderer/src/features/script/ui/useScriptSceneIndex.ts`, `src/renderer/src/features/script/ui/ScriptSceneList.tsx`, `src/renderer/src/features/script/model/build-script-scene-summary-index.ts`, `src/renderer/src/features/script/model/script-scene-quality-audit.ts`, `related tests`]

- [ ] 9. Make lint scope truthful and add the missing file-scoped regression tests required by the failed acceptance

  **What to do**: Fix repository lint scope so temporary directories such as `.codex/tmp-hook-check/*` and `.tmp-node-tests/*` no longer keep `npm run lint` red. Use a durable repo rule such as ignore configuration, not manual cleanup expectations. In the same wave, add all required file-scoped Node tests missing from the failed acceptance: `derive-active-character-blocks.test.ts`, `build-detailed-outline-view-model.test.ts`, `build-script-scene-summary-index.test.ts`, `script-scene-quality-audit.test.ts`, and `script-plan-service.test.ts`. If `build-detailed-outline-view-model.ts` does not yet exist, create the minimal production file needed to satisfy the explicit plan/test contract before writing its test.
  **Must NOT do**: Do not waive lint failures as “temp only,” do not hide test debt by weakening the gate, and do not skip the detailed-outline view-model test because its production file is missing.

  **Recommended Agent Profile**:
  - Category: `quick` — Reason: scoped repo hygiene and targeted tests are straightforward once the architecture decisions are settled.
  - Skills: [`test-driven-development`] — required because missing tests are part of the acceptance failure itself.
  - Omitted: [`verification-loop`] — final verification happens later; this task is about making the gate truthful.

  **Parallelization**: Can Parallel: YES | Wave 4 | Blocks: 10 | Blocked By: 4, 7, 8

  **References** (executor has NO interview context — be exhaustive):
  - Verification source: `docs/plans/计划总表.md:624-654` — explicit missing tests and grep gates called out in the current project plan.
  - Pattern: current lint failure excerpt shows `.codex/tmp-hook-check/*` causing prettier/eslint warnings and must be formally excluded or cleaned by rule.
  - Test targets required by failed acceptance:
    - `src/renderer/src/features/character/model/derive-active-character-blocks.test.ts`
    - `src/renderer/src/features/detailed-outline/model/build-detailed-outline-view-model.test.ts`
    - `src/renderer/src/features/script/model/build-script-scene-summary-index.test.ts`
    - `src/renderer/src/features/script/model/script-scene-quality-audit.test.ts`
    - `src/renderer/src/app/services/script-plan-service.test.ts`
  - Pattern: `src/renderer/src/features/detailed-outline/ui/DetailedOutlineStage.tsx` still contains inline heavy computation and needs a corresponding view-model abstraction under test.

  **Acceptance Criteria** (agent-executable only):
  - [ ] `npm run lint` passes.
  - [ ] All five required `node --test ...` commands pass.
  - [ ] `npm run typecheck` passes.
  - [ ] `npm run build` passes.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Lint scope truthfulness gate
    Tool: Bash
    Steps: Run `npm run lint` after adding durable ignore/config support for temporary directories and committing the required test files.
    Expected: Lint passes without requiring manual deletion of temp directories.
    Evidence: .sisyphus/evidence/task-9-lint-pass.txt

  Scenario: Required file-scoped tests gate
    Tool: Bash
    Steps: Run the five required `node --test` commands defined in the plan Definition of Done.
    Expected: All five tests pass, proving acceptance debt is paid down.
    Evidence: .sisyphus/evidence/task-9-required-tests.txt
  ```

  **Commit**: YES | Message: `test(repo): restore truthful lint and targeted regression gates` | Files: [`eslint/prettier ignore config`, `required *.test.ts files`, `minimal production files needed for those tests`]

- [ ] 10. Re-run deterministic acceptance gates and record performance evidence only after correctness gates are green

  **What to do**: After tasks 1-9 are complete, execute the full deterministic acceptance sequence in repo order: `npm run typecheck`, `npm run lint`, `npm run build`, all required `node --test` files, then the grep gates for duplicate handlers, full-snapshot bypass removal, centralized `changeProjectStage`, project-name bug removal, and script audit isolation. Only after those correctness/static gates pass may the implementer collect performance evidence for project-open and stage-switch flows and update the worklog/evidence files with the authoritative labels. Document the exact measured commands and outputs under `.sisyphus/evidence/`.
  **Must NOT do**: Do not record performance victory before the correctness gates are green, and do not skip grep/static audits because builds pass.

  **Recommended Agent Profile**:
  - Category: `deep` — Reason: this is the release/acceptance gate for the whole repair program and must be executed rigorously.
  - Skills: [`verification-loop`] — final deterministic verification workflow fits the skill directly.
  - Omitted: [`webapp-testing`] — user manual test remains outside AI scope by project rule.

  **Parallelization**: Can Parallel: NO | Wave 4 | Blocks: Final Verification Wave | Blocked By: 2, 3, 4, 5, 6, 7, 8, 9

  **References** (executor has NO interview context — be exhaustive):
  - Definition of Done section in this plan — exact commands required before acceptance can be reopened.
  - Evidence rule: `.sisyphus/evidence/` — store outputs for each gate with task-numbered filenames.
  - Perf labels required by the current project plan: `open_project_session`, `stage_session_switch`, `stage_store_hydrate`, `active_character_blocks_derive`, `detailed_outline_view_model`, `script_scene_summary_index`, `script_scene_quality_audit`, `script_plan_service`, `first_interactive`.
  - Current project plan source: `docs/plans/计划总表.md:503-529` — label list and evidence expectation.

  **Acceptance Criteria** (agent-executable only):
  - [ ] Every command in this plan’s Definition of Done passes and evidence is captured.
  - [ ] Grep gates prove no forbidden legacy authority path remains.
  - [ ] Performance evidence is recorded only after all correctness/static gates pass.

  **QA Scenarios** (MANDATORY — task incomplete without these):

  ```
  Scenario: Full deterministic repo acceptance run
    Tool: Bash
    Steps: Run the complete command sequence from this plan’s Definition of Done and save each output into `.sisyphus/evidence/`.
    Expected: All commands pass; no gate is skipped or hand-waved.
    Evidence: .sisyphus/evidence/task-10-full-acceptance-run.txt

  Scenario: Post-fix grep authority audit
    Tool: Bash
    Steps: Run the grep commands for handler uniqueness, full-snapshot removal, centralized changeProjectStage, project-name bug removal, and script audit isolation.
    Expected: All grep gates meet the exact constraints defined in this plan.
    Evidence: .sisyphus/evidence/task-10-post-fix-grep-audit.txt
  ```

  **Commit**: YES | Message: `chore(acceptance): rerun deterministic remediation gates` | Files: [`.sisyphus/evidence/*`, `4.worklog.md if project process requires recording evidence summary`]

## Final Verification Wave (MANDATORY — after ALL implementation tasks)

> 4 review agents run in PARALLEL. ALL must APPROVE. Present consolidated results to user and get explicit "okay" before completing.
> **Do NOT auto-proceed after verification. Wait for user's explicit approval before marking work complete.**
> **Never mark F1-F4 as checked before getting user's okay.** Rejection or user feedback -> fix -> re-run -> present again -> wait for okay.

- [ ] F1. Plan Compliance Audit — oracle
- [ ] F2. Code Quality Review — unspecified-high
- [ ] F3. Real Manual QA — unspecified-high
- [ ] F4. Scope Fidelity Check — deep

## Commit Strategy

- Commit 1: authority tests + handler uniqueness fixes
- Commit 2: stage session rollout + duplicate writer removal
- Commit 3: activeCharacterBlocks producer + stale path cleanup
- Commit 4: plan fingerprint + script audit path fixes
- Commit 5: lint scope + targeted tests + final verification evidence

## Success Criteria

- 验收结论从 REJECT 转为可复核通过
- 所有当前已确认发现都有对应代码修复、测试与静态证据
- 重新验收建立在单一 authority 路径与 truthful invalidation 上，而不是口头解释
