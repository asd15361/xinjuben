# 聊天记录

这个文件只保留一条当前主线。

断线恢复顺序固定：

1. 先看 `聊天记录.md`
2. 再看 `项目原则.md`
3. 最后看 `AI自我学习.md`

## 当前主线

- 当前正在做：把“确认信息 -> 粗纲/人物”这条主链这轮已经收口的结果固化下来，防止下轮又从旧脏数据、旧入口和旧判断重新开始。
- 当前判断：这一轮已经证明，粗纲/人物反复失败的主因不是单点 timeout，而是第二口径、双来源和自动推进耦合；这几个点收住后，真实软件链路能正常走通。
- 当前结论：这轮 5 个动作已经全部落地并通过真实 Electron 复测：
  - 粗纲入口不再二次 `summarizeChatForGeneration()`，只认确认过的 `storyIntent`。
  - `outline-facts.ts` 不再系统补写正式 `facts`，只做格式转换。
  - `activeCharacterBlocks` 改成按 `outline + characterDrafts` 现算，characters shard 不再持久化这一套快照。
  - 人物合同已经统一到一套 predicate，`guardianEnforceCharacterSave()` 已接进保存链。
  - 自动推进已临时停掉，旧测试项目污染已清掉，再用干净环境跑了真实软件复测。
- 下一步固定按这个顺序接：
  1. 继续盯“确认信息 -> 粗纲/人物”这条链是否会从旧兼容桥重新长出口子。
  2. 再往下处理“人物 -> 详纲 -> 剧本”链路，重点查正式 facts 承接和 stage contract 是否还有第二裁判。
  3. 所有真实复测继续先清污染，再跑干净项目，不再拿旧脏项目当判据。

## 还没解决的重大问题

- `summarize-chat-for-generation-support.ts` 和仓库里一批旧 fallback/support 文件还在，当前主链虽然不再吃，但仍有回流风险。
- 详纲和剧本阶段还没有按这轮新主权完整复测，尤其正式 facts 承接、人物合同下游消费、script stage gate 还要继续盯。
- `useProjectAutoGeneration.ts` 现在是 no-op 临时止血，不是长期设计，后面要在不制造第二主线的前提下决定是否恢复。
- `generationBriefText` 目前已经不再主导粗纲生成入口，但其他下游如果还把它当第二真相，也要继续清。

## 最近进展

### 2026-03-27 当前轮

- 问题：粗纲/人物为什么修了很多次还反复炸。
- 解决：这轮按固定顺序把 5 个根因点全部收口：
  - 删掉粗纲入口里的二次 `summarizeChatForGeneration()`。
  - 删掉系统补写正式 `facts` 的逻辑。
  - 收死 `activeCharacterBlocks` 主权，只保留派生链。
  - 统一人物合同，并把 guardian 接进保存路径。
  - 暂停自动推进，清掉旧测试污染，再跑真实复测。
- 状态：代码收口后，`typecheck:node`、`typecheck:web` 和相关板块测试都过了。

- 问题：新的主链到底有没有在真实软件里跑通。
- 解决：用干净隔离 `userData` 跑了真实 Electron 复测，链路是：
  - 新建项目
  - 输入正式创作信息
  - 点击“我聊完了，确定信息”
  - 点击“生成第一版粗纲和人物”
- 状态：真实复测成功：
  - 确认信息返回的是单一 10 集合同，没有再混出 30 集。
  - 粗纲成功生成 10 集、5 条 facts、4 个人物。
  - 项目 stage 落在 `character`，`activeCharacterBlocks` 只有 1 套派生结果。
  - 日志显示 `story_intake` 约 19.8 秒完成，`rough_outline` overview 约 23.8 秒、10 集 batch 约 51.0 秒、`character_profile` 约 46.7 秒，全部走 `deepseek-chat`。

- 问题：旧脏项目会不会继续影响判断。
- 解决：已清掉实际工作区里的 3 个空聊天测试项目和污染项目 `project_mn1d9gkd`。
- 状态：当前实际工作区只剩两个旧项目，复测判断不再被这批脏数据拖偏。

## 关键证据

- 真实复测隔离目录：`tools/e2e/out/userdata-real-confirm-outline-mn8f3z2i`
- 真实复测项目：`project_mn8f413c`
- 真实确认信息结果明确写成 `【项目】修仙传｜10集`，且 `confirmContains30=false`
- 真实生成结果：`outlineCount=10`、`characterCount=4`、`factCount=5`、`activeCharacterBlocks=1`
- 真实运行日志显示：
  - `story_intake`：`elapsedMs=19839`
  - `rough_outline_overview`：`elapsedMs=23754`
  - `rough_outline_batch 1-10`：`elapsedMs=50970`
  - `character_profile`：`elapsedMs=46724`
  - lane / model 全部是 `deepseek / deepseek-chat`

## 已完成摘要

- 2026-03-26：文档体系已收成 `聊天记录.md`、`项目原则.md`、`AI自我学习.md` 三份当前工作区文档；旧任务卡、阶段报告、测试裁判口径已归档。
- 2026-03-26：AI runtime 已收成 `DeepSeek` 单通道，不再保留第二模型 lane。
- 2026-03-26：确认信息已从“双 JSON 合同”改成“单对象合同 + 本地渲染 generationBriefText + 诊断日志”。
- 2026-03-26：粗纲主链已从“一次性整季大 JSON”拆成“overview + 10 集 batch”。
- 2026-03-26：窗口刷新、重发和销毁时，主进程生成任务已接上中断，不再允许旧任务继续偷偷落盘。
- 2026-03-27：粗纲入口的二次 summarize、系统补 facts、`activeCharacterBlocks` 双来源、人物合同双口径、自动推进干扰，这 5 个点已经按顺序收口。
- 2026-03-27：真实 Electron 复测已跑通“确认信息 -> 粗纲/人物”主链，并采到干净日志。

## 维护规则

1. 只保留一条当前主线，不并列两套“我们现在在做什么”。
2. 最近 3 天保留细节；3 天前开始压缩；7 天前只留摘要。
3. 用户发“存档”时，立刻整理这份文件，不等轮次。
4. 这份文件必须持续保留三类内容：还没解决的重大问题、当前最新进展、这轮新确认的重大发现。
