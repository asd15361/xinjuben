# 计划总表

状态：进行中
更新时间：2026-03-25
当前主线：foundation 红灯分流与治理（recovery lifecycle 已完成技术收口）
当前执行口径：`先确认 recovery 本体不再回退 -> 再分别处理 formal 真失败与 visible seeded runner 治理 -> 文档与门禁只保留一套正式口径`

## 当前总判断

- `renderer dynamic import recovery lifecycle` 当前已经完成技术收口，不再是现役阻塞。
- `quality` 已正式化并通过，当前不再允许写成 `not_ready`。
- `foundation-verdicts` 仍未全绿，但剩余红灯已经分流清楚：
  1. `formal` 真失败：人物正式 parser 未覆盖 `roleLayer` / `activeBlockNos`。
  2. `visible` 真失败：`p0-real-regression-v1` seed 被现行 formal-fact gate 拒绝，表现为 `p0_start_button_disabled`。
- 用户手测当前只保留为体验复核，不再充当 recovery 任务的主权门槛。

## 已完成的官方收口

### 1. Recovery Lifecycle

- `failure-only lifecycle + explicit success ACK` 已落地。
- `AppShell` 的 `HomeShell` 成功挂载与 `StageViewport` 的当前 active stage 成功挂载是唯一 success ACK。
- `window load`、`document.readyState`、root render、`ProjectShell` 不再充当成功。
- 官方 probe：`node tools/e2e/dynamic-import-recovery-lifecycle-probe.mjs` 已通过。
- recovery 单测：`node --test --experimental-strip-types src/renderer/src/app/utils/dynamic-import-recovery.test.ts` 已通过。
- MAIN repo `npm run typecheck` 已通过。
- MAIN repo `npm run build` 已通过。

### 2. Quality Gate

- `quality` 已正式化为官方可运行层。
- `npm run verify:quality`、`npm run authority:check`、`node tools/e2e/quality-gate.mjs` 均通过。
- `tools/e2e/README.md`、`tools/e2e/runner-taxonomy.mjs`、`tools/e2e/foundation-verdicts.mjs`、`package.json` 当前已经对齐同一套质量门禁定义。

## 当前剩余红灯

### A. Formal

- 根因：`parseCharacterBundleText` 未覆盖 `roleLayer` / `activeBlockNos`，而正式 `CharacterDraftDto` 与下游 planning blocks 已经把这两个字段当成现役正式合同。
- 当前事实：这是现役合同断裂，不是历史文档残影。
- 直接证据：`node tools/e2e/contract_guard_check.mjs` 当前失败点明确落在该 parser 漏字段。

### B. Visible

- 根因：`p0-real-regression-v1` seed 的 `outlineDraft.facts` 仍是 `ai_suggested + draft`，不满足现行 formal-fact gate 的 `user_declared + confirmed + declaredStage=outline` 条件。
- UI 表现：script 启动按钮 disabled，runner 记为 `p0_start_button_disabled`，再被分类为 `product_failure`。
- 当前判断：这更接近 seeded runner 与现行 gate 失配，不是 recovery 本体失败。

## 当前分线

### Wave A：Formal Contract Parity

- 目标：补齐人物正式 parser 对 `roleLayer` / `activeBlockNos` 的解析与归一化。
- 完成定义：`node tools/e2e/contract_guard_check.mjs` 通过，`foundation-verdicts.formal = pass`。

### Wave B：Visible Runner Governance

- 目标：为 `electron_p0_real_regression.mjs` 做唯一真相决策。
- 二选一：
  1. 升级 seed，使其满足现行 formal-fact gate。
  2. 调整 runner 分类或退役旧 seed 口径，避免继续把“seed 撞 gate”误报成产品失败。
- 完成定义：`foundation-verdicts.visible` 不再是当前这条 `product_failure`。

### Wave C：文档与门禁同步

- 目标：计划、任务卡、worklog、probe、quality gate 只保留一套正式口径。
- 当前状态：recovery 与 quality 已同步；formal / visible 的后续结论必须继续回填。

## 当前门禁

1. `node tools/e2e/dynamic-import-recovery-lifecycle-probe.mjs`：通过
2. `node tools/e2e/quality-gate.mjs authority:check`：通过
3. `node tools/e2e/quality-gate.mjs`：通过
4. `npm run typecheck`：通过
5. `npm run build`：通过
6. `node tools/e2e/contract_guard_check.mjs`：失败
7. `node tools/e2e/electron_p0_real_regression.mjs`：失败
8. `node tools/e2e/foundation-verdicts.mjs`：`probe=pass / quality=pass / formal=fail / visible=fail`

## 进度口径

- 总任务进度：85%
- 当前 recovery 小任务：100%
- 当前项目剩余阻断：`formal` 与 `visible` 两条并行后续主线

## 禁止事项

- 不得再把 recovery 任务写成“仍未开始”或“只差手测”。
- 不得再把 `quality` 写成 `not_ready`。
- 不得把 `formal` 的 parser 漏字段和 `visible` 的 seeded runner 失配混成一个问题。
- 不得为了让 probe 变绿而回退显式 success ACK 实现。
