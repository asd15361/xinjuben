# 创作主链结构验收记录（2026-03-20）

## 一句话结论
当前主链第一轮统一已经成立，`10集规划 / 10集详纲 / 5集写作 / 逐集细纲真源` 四个关键点已经落到代码和文档里；现在可以把这份记录当成恢复测试前的结构门槛，但恢复放量测试前，仍要先把本记录同步进任务卡和 worklog。

## 验收范围
- 只验结构，不验内容质量
- 只验唯一真相，不拿旧链路结果充数
- 只看四件事：`10集规划单位`、`10集详纲单位`、`5集剧本写作单位`、`逐集细纲真源`

## 1. 10集规划单位
### 当前结论
成立。

### 代码依据
- `src/main/application/workspace/generate-outline-and-characters.ts`
- `src/shared/domain/workflow/planning-blocks.ts`

### 文档依据
- `AGENTS.md`
- `2.rules.md`
- `docs/system-authority（系统定义权与主权规则）.md`
- `docs/plans/创作全链路纠偏与边界重建计划（第一版）.md`

### 说明
当前粗纲层已经不是整季一口气平铺到底，而是明确按 `10集` 为规划块组织。整季方向还保留，但真正往下游传递时，已经有 `outlineBlocks` 这个块结构。

### 是否还有残口
有轻微残口。

### 残口判断
旧字段和旧整季表述还在兼容层里共存，历史数据读取时仍可能看到整季平铺痕迹。但主链真源已经不是旧口径，残口属于历史兼容，不属于当前主链定义权。

## 2. 10集详纲单位
### 当前结论
成立。

### 代码依据
- `src/main/application/workspace/generate-detailed-outline.ts`
- `src/shared/domain/workflow/planning-blocks.ts`
- `src/renderer/src/features/detailed-outline/ui/DetailedOutlineStage.tsx`
- `src/renderer/src/components/DetailedOutlineActsPanel.tsx`

### 文档依据
- `AGENTS.md`
- `docs/system-authority（系统定义权与主权规则）.md`
- `docs/plans/创作全链路纠偏与边界重建计划（第一版）.md`

### 说明
详细大纲现在跟着粗纲块走，默认一个粗纲块对应一个 `10集` 详纲块。内部段落也不再死锁成固定四段，而是按块内集数自动拆段。

### 是否还有残口
有轻微残口。

### 残口判断
旧字段名里仍保留 `segments` 这种历史表达，容易让人误以为还是旧四段逻辑。但当前生成逻辑和展示逻辑已经以 `detailedOutlineBlocks + 逐集细纲` 为主。

## 3. 5集剧本写作单位
### 当前结论
成立。

### 代码依据
- `src/main/application/script-generation/plan/resolve-runtime-profile.ts`
- `src/shared/domain/workflow/planning-blocks.ts`
- `src/main/application/script-generation/prompt/create-script-generation-prompt.ts`

### 文档依据
- `AGENTS.md`
- `2.rules.md`
- `docs/system-authority（系统定义权与主权规则）.md`
- `docs/plans/创作全链路纠偏与边界重建计划（第一版）.md`

### 说明
剧本层的业务单位已经固定成 `5集`。runtime 现在只能在这个框架里调度，不能再反过来决定创作工序。当前批次上下文也已经明确存在，不再是“表面一批、实际单集各吃各的散上下文”。

### 是否还有残口
有轻微残口。

### 残口判断
单集仍然是逐集调用模型落地，这本身没问题；真正要盯的是它是否都吃同一份 `5集` 批次前情和目标。目前从 prompt 结构看已经收口，但恢复测试时要重点验这一点是否稳定。

## 4. 逐集细纲真源
### 当前结论
基本成立，这是当前最关键的一项。

### 代码依据
- `src/shared/domain/workflow/planning-blocks.ts`
- `src/main/application/script-generation/prompt/create-script-generation-prompt.ts`
- `src/main/application/workspace/generate-detailed-outline.ts`

### 文档依据
- `AGENTS.md`
- `docs/system-authority（系统定义权与主权规则）.md`
- `docs/plans/创作全链路纠偏与边界重建计划（第一版）.md`

### 说明
当前剧本当前集任务已经明确从逐集细纲里取数，不再允许 runtime、UI、repair 随意改写当前集任务。换句话说，剧本当前集现在已经有“唯一真源”。

### 是否还有残口
有一个需要继续盯的残口。

### 残口判断
当前查找逻辑还兼容 `detailedOutlineBlocks` 和历史 `segments` 两种入口，这说明兼容桥还没完全退干净。它现在是为了兼容旧数据，不是第二真相；但只要双入口还在，后面测试就必须继续盯有没有旧口径回流。

## 双层人物结论
### 当前结论
成立。

### 说明
人物已经不是一口气整包硬塞到底，而是分成：
- `核心角色包`
- `当前块活跃角色包`

这件事和 `10集规划 / 10集详纲 / 5集写作` 已经形成同一套主链。

## 结构风险清单
- 最大风险不是业务单位没定，而是历史兼容字段还在，容易让后来的人误会还有第二口径。
- 最大残口不是写作批次，而是“逐集细纲真源”虽然已经成立，但兼容桥还没完全退成纯历史层。
- 当前不要拿旧测试结果冒充新主链结果。

## 是否允许恢复测试
允许恢复，但只允许从 `10集` 档开始恢复。

## 恢复测试前提
- 这份结构验收记录已同步到任务卡
- 这份结构验收结论已同步到 `4.worklog.md`
- 恢复测试时按 `10 -> 20 -> 30 -> 40 -> 50 -> 60` 分档进行
- 每一档都要单独记录耗时、通过率、平均字数、失败类型
- 一旦发现旧口径回流，先修主链，不拿 repair 或硬 gate 顶上去
