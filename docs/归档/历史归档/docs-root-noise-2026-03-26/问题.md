# 项目问题审计报告

更新时间：2026-03-21

---

## 当前分层总览（便于上线判断）

> 关联文档：`docs/script_60长测失败原因映射.md`。如果要快速知道“现在为什么还不能上线”，优先看这份映射文档。

> 当前联合状态（2026-03-21 最新同步）：**还不能说“可以上线”**。B 侧可验收项已累计收掉 9 项并验收通过；A 侧主链已推进到 `script_60` 真实结果进一步逼近过门，且 `mockEvidence = false`。但当前上线标准仍是“稳定产出 60 集优质剧本”，这条标准还没有被最终真实长测证明通过。

> 当前最关键事实（2026-03-21 本轮更新）：`script_button_disabled_in_ui` 已不再是主卡点。A 侧已补上 `plan.ready / blockedBy` 真因透出，并把 `详细大纲` 长流程改成“按块增量持久化 + 基于已有块恢复”。当前最新真阻塞不再是 UI，而是**还缺基于这条新链路的 60 集新鲜正式证据**。

> 已归因旧样本：此前的 **944.9s / exit 1** 异常样本，以及后续那轮 **904.3s** 仍卡在场次数/overlong 的样本，当前都已被最新这轮 **1071.5s** 的真实验收结果覆盖；现在应以“只剩第 2 集集尾钩子偏弱”作为唯一主参考阻塞项。

### 上线门禁表（建议直接按这个判断）

| 分类            | 含义                              | 当前内容                                                                                                                                                                                                                                                  |
| --------------- | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 必阻塞          | 不解决就不应上线                  | 真实 `script_60` 长测尚未基于最新链路重新正式 pass、`详细大纲` 长流程的新恢复能力还没拿到新鲜长测证据、模型路由硬编码 deepseek、主链 repair / runtime 稳定性尚未被最新失败样本完全证明                                                                                |
| 可带病          | 可以上线，但应排期继续清理        | Fire-and-forget void 模式、Store 错误链处理不透明、系统指令优先级无统一约束、少量重复 helper / 类型重复、历史调试残留                                                                                                                                     |
| 已解决 / 已裁掉 | 已收口，或经核对属于过时/失真描述 | ETA 第二口径、进度展示第二口径、chat 页本地 status 第二张嘴、renderer 侧 `as any`、renderer `console.error` 残留、首页原生 `window.confirm` 第二风格入口、第二模型 fast lane 默认值 BUG、解析失败“完全无自救”、structured-chain / structured-inference 循环依赖 |
| 历史项          | 保留记录，不与当前门禁混看        | 下文完整审计正文                                                                                                                                                                                                                                          |

### A. 已解决 / 已裁掉（不再阻塞当前上线判断）

1. **前端 ETA 第二口径**
   - `useChatStageActions.ts`
   - `useDetailedOutlineStageActions.ts`
   - `useScriptStageActions.ts`
   - `useProjectAutoGeneration.ts`
   - 已统一走 `src/renderer/src/app/utils/stage-estimates.ts`

2. **进度展示第二口径 / 误导性展示**
   - `ProjectGenerationBanner.tsx`
   - `AppHeader.tsx`
   - `useProjectGenerationProgress.ts`
   - 已统一展示“已处理 / 预计 / 预计剩余或已超预计”，并明确 ETA 仅用于前端展示估算

3. **chat 页本地 status 第二张嘴**
   - `useChatStageActions.ts`
   - `ChatStage.tsx`
   - `ChatIntakePanel.tsx`
   - 已移除与全局 `generationStatus / generationNotice` 冲突的本地状态口径

4. **renderer 侧类型脏口子**
   - `ChatIntakePanel.tsx` 的 `free_chat as any` 已移除
   - `ScriptStage.tsx` 的 `audit: { report: null } as any` 已替换为真实 `useScriptAudit()`

5. **经核对属于过时或失真的问题描述**
   - “解析失败无自救”——当前 `run-script-generation-batch.ts` 已有 `buildParseRecoveryPrompt` 和二次尝试
   - “第二模型 fast lane 默认值 BUG”——当前 `provider-config.ts:44` 已是 `第二模型_FLASH_MODEL || '第二模型-2.5-flash'`
   - `structured-chain.ts ↔ structured-inference.ts` 循环依赖——当前文件已不存在

### B. 真正阻塞上线（A 主链继续处理）

1. **真实 `script_60` 长测还没稳定过门**
   - 当前最新状态：能真实生成 5 集正式剧本，`mockEvidence = false`，形式质量已基本过线，`legacyFormat = null/false`，且 `postflightIssues = 0`；但“稳定产出 60 集优质剧本”尚未被最终长测证明。
   - 当前剩余失败已收缩到：**只剩第 2 集集尾钩子偏弱**。

2. **单集失败会直接中断整批生成**
   - 代码证据：`run-script-generation-batch.ts:116-126`
   - 当前任一集失败都会 `throw ScriptBatchGenerationError(...)`，整批退出。

3. **模型路由硬编码 deepseek**
   - 代码证据：`runtime-policy.ts:77-89`，以及 `main/ipc/ai-handlers.ts:13`
   - 当前 `enabledLanes` 会先被过滤成只剩 `deepseek`，多通道路由设计几乎不起作用，而且入口层也有同类 deepseek-only 过滤。

4. **主链 repair / runtime 稳定性还没被真实失败样本证明**
   - 重点在 repair 接受策略、本地稳定收口逻辑、以及真实失败点（例如 `script_60`）闭环是否完成。

### C. 症状级问题（可带病上线，但应排期继续清理）

1. **Store 错误链处理不透明**
   - `project-store-core.ts:15-24`
   - 现状不是“完全静默吞错”，但 `.catch(() => undefined)` 仍会吞掉前链错误，调用方感知弱。

2. **Fire-and-forget void 模式**
   - renderer 多处仍存在 `void ...` 调用
   - 会放大失败不可见，但不是当前最底层根因。

3. **系统指令优先级缺乏统一定义**
   - `ai-provider-invoke.ts`
   - 会影响长期一致性，但不是当前第一序上线阻塞。

4. **重复 helper / 类型重复 / 第二个真相中的一部分**
   - 例如少量重复解析 helper、返回类型不一致的 fallback builder
   - 更偏维护成本，而不是马上毁掉作品产出的首要问题。

### D. 历史审计项（保留记录，需按优先级继续清理）

- 脏名污染
- 第二个口子
- 第二个真相
- 配置漂移中的非展示层问题
- 架构问题
- AI 推理质量问题
- 数据一致性问题
- 剧情质量系统问题

---

## 一、脏名污染（Name Pollution）

### 1. 重复函数定义（同一逻辑，多处定义）

| 函数名                                 | 文件A                                            | 文件B                                                              | 问题                                    |
| -------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------ | --------------------------------------- |
| `inferChainFromStructuredSections`     | `structured-chain.ts:142`                        | `structured-inference.ts:150`                                      | inference文件只是wrapper，调用chain实现 |
| `inferCharacterLayersFromSections`     | `structured-chain.ts:158`                        | `structured-inference.ts:160`                                      | 同上                                    |
| `buildFallbackStoryIntent`             | `generate-outline-and-characters-support.ts:204` | `summarize-chat-for-generation-fallback-builders.ts:87`            | 返回类型不同（Dto vs Partial）          |
| `EpisodeStageWindow` 接口              | `stage-guidance.ts:3`                            | `archive/build-script-task-cards.ts:14`                            | 归档目录中的文件还有旧定义              |
| `cleanJsonLikeText` + `tryParseObject` | `summarize-chat-for-generation-shared.ts:29-57`  | `generate-outline-and-characters-support.ts:51-85`                 | 完全重复                                |
| `normalizeAnchorName`                  | `story-contract-policy.ts`                       | `summarize-chat-for-generation-shared.ts` (as `cleanPossibleName`) | 同功能不同名                            |

### 2. 循环依赖

```
structured-chain.ts:7  → imports from structured-inference.ts
structured-inference.ts:10 → imports from structured-chain.ts
```

### 3. 调试代码残留

- `_tmp_write_card.js` - 根目录临时文件
- `console.log` 散落6处：
  - `_tmp_write_card.js:11,13,15,16`
  - `preload/index.ts:24`
  - `system-handlers.ts:5`
  - `project-store-core.ts:30`
  - `useRuntimeInfo.ts:29`
  - `generate-detailed-outline.ts:292`

### 4. @ts-ignore 类型绕过

- `preload/index.ts:27,29` - 绕过类型检查

---

## 二、第二个口子（Multiple Entry Points）

### 同一数据多处修改入口

| 数据                 | 入口A                                 | 入口B                                          |
| -------------------- | ------------------------------------- | ---------------------------------------------- |
| `outlineDraft.facts` | `formal-fact-handlers.ts:declare`     | `formal-fact-handlers.ts:confirm`              |
| `generationStatus`   | `useScriptStageActions.ts:50,117,204` | `useProjectAutoGeneration.ts:14,75,94,154,228` |
| `storyIntent`        | `useChatStageActions.ts:68`           | `useHomePageActions.ts:41`                     |
| `scriptDraft`        | `useScriptStageActions.ts:147,154`    | `useProjectAutoGeneration.ts:201,205`          |
| `outline`            | `saveOutlineDraft`(store:122)         | `saveOutlineAndCharacters`(store:182)          |

### Store层5个Save函数都调用同一个updateProject

```
saveOutlineDraft → updateProject
saveCharacterDrafts → updateProject
saveDetailedOutlineSegments → updateProject
saveScriptDraft → updateProject
saveOutlineAndCharacters → updateProject
```

无事务性保障。

---

## 三、第二个真相（Second Sources of Truth）

### 1. StageContractType类型三处重复定义

```typescript
// stage-contract.ts:9
export type StageContractType = 'outline' | 'character' | 'detailed_outline' | 'script'

// input-contract-handlers.ts:18 — 内联重复
// preload/api/workflow/stage-contract.ts:45 — 内联重复
```

### 2. FormalFact类型重复

```typescript
// formal-fact.ts:1-4 定义基础类型
// workflow.ts:35-48 包含完整接口（重复）
```

### 3. 归档目录5个.ts文件从未被引用

`docs/archive/orphaned-2026-03-19/`:

- `build-script-task-cards.ts` — 导出5个函数
- `build-script-relay-block.ts` — 导出1个函数
- `detailedOutlineReadiness.ts` — 导出2个函数
- `detailed-outline-coverage.ts` — 导出2个函数
- `useFormalProjectSnapshot.ts` — 导出1个React hook

这些文件只被自己目录内部引用，主代码库0引用，且有模块找不到的LSP错误。

---

## 四、Async/Promise错误处理

### 1. Promise链静默吞错误（严重）

**`project-store-core.ts:16-20`:**

```typescript
storeOpChain = next.then(
  () => undefined, // 成功也返回undefined
  () => undefined // 失败也返回undefined — 静默失败！
)
```

### 2. Fire-and-forget void模式（中等）

| 文件                          | 行号    | 代码                                                  |
| ----------------------------- | ------- | ----------------------------------------------------- |
| `useProjectAutoGeneration.ts` | 74, 153 | `void (async () => {...})()` 无catch                  |
| `useScriptStageActions.ts`    | 118     | `void window.api.workspace.saveGenerationStatus(...)` |
| `useHomePageActions.ts`       | 101     | `void reload()`                                       |
| `useStageReadiness.ts`        | 35      | `void validate()`                                     |
| `useStageContractSummary.ts`  | 71      | `void syncContracts()`                                |
| `useScriptGenerationPlan.ts`  | 43      | `void buildPlan()`                                    |
| `useScriptLedgerPreview.ts`   | 34      | `void load()`                                         |
| `useRuntimeInfo.ts`           | 33      | `void loadRuntimeInfo()`                              |

---

## 五、配置漂移（Config Drift）

> B 侧状态（2026-03-21）：前端展示层 ETA / progress 已收口到单一口径。当前 `useChatStageActions.ts`、`useDetailedOutlineStageActions.ts`、`useScriptStageActions.ts`、`useProjectAutoGeneration.ts` 已统一走 `src/renderer/src/app/utils/stage-estimates.ts`；`ProjectGenerationBanner.tsx`、`AppHeader.tsx`、`useProjectGenerationProgress.ts` 已统一展示“已处理 / 预计 / 预计剩余或已超预计”，并明确说明 ETA 仅用于前端展示估算。另：chat 页本地 `status` 第二张嘴已移除，`ChatIntakePanel.tsx` 中的 `free_chat as any` 类型绕过也已收掉。

### 1. 进度估算时间——同一概念多处不同值

**DETAILED_OUTLINE_ESTIMATED_SECONDS：**

| 文件                                  | 值      |
| ------------------------------------- | ------- |
| `stage-estimates.ts:2`                | 70      |
| `useProjectAutoGeneration.ts:8`       | 90      |
| `useDetailedOutlineStageActions.ts:6` | **300** |

**OUTLINE_ESTIMATED_SECONDS：**

| 文件                       | 值      |
| -------------------------- | ------- |
| `stage-estimates.ts:1`     | 104     |
| `useChatStageActions.ts:6` | **160** |

### 2. Timeout值分散

| 位置                                               | 值         |
| -------------------------------------------------- | ---------- |
| `provider-config.ts:39,46,53` (AI)                 | 45000      |
| `provider-config.ts:61` (runtimeFetch)             | 15000      |
| `generate-outline-and-characters-support.ts:15-16` | 45000      |
| `generate-detailed-outline.ts:36`                  | **300000** |

### 3. 第二模型 fast lane默认值BUG

**`provider-config.ts:44`:**

```typescript
model: process.env.DEEPSEEK_MODEL || 'deepseek-chat',  // ← 错误！应该是第二模型_FLASH_MODEL
```

### 4. Retry逻辑分散

| 常量                            | 位置                              | 值  |
| ------------------------------- | --------------------------------- | --- |
| `DETAILED_OUTLINE_MAX_ATTEMPTS` | `generate-detailed-outline.ts:37` | 2   |
| `MAX_QUALITY_REPAIR_PASSES`     | `repair-generated-scenes.ts:14`   | 2   |
| Store重试次数                   | `project-store.ts:57`             | 5   |

### 5. 环境变量

- `.env` 和 `.env.example` 存在
- `XINJUBEN_USER_DATA_DIR` 在 `main/index.ts:13` 使用但未在任何配置中定义
- `E2E_USER_DATA_DIR` 在两处使用：`main/index.ts:13` 和 `project-store-core.ts:26`

---

## 六、架构问题

### 1. 82个application层文件

```
src/main/application/workspace/ — 大量工作流文件，职责不清
src/main/application/script-generation/ — 剧本生成，职责不清
```

### 2. IPC Handler暴露9个独立端点

`workspace-project-handlers.ts` 每个save函数都是独立IPC端点，直接操作store。

### 3. Zustand Store有17个set方法

`useStageStore.ts:124-156` — 状态管理粒度过细。

---

## 七、工作流瓶颈问题

### 1. 状态机问题

**`state-machine.ts:57`** — 任何未处理事件类型都会导致failed状态：

```typescript
return markBatchStatus(board, 'failed', event.reason) // 无日志
```

**`state-machine.ts:46`** — 单集失败导致整批失败：

```typescript
if (event.type === 'episode_failed') {
  return markBatchStatus(next, 'failed', event.reason) // 级联失败
}
```

### 2. 重试逻辑问题

**`run-script-generation-batch.ts:70-72`** — 解析失败直接抛错无自救：

```typescript
// 不保底，parse 失败就直接抛错，不自救
const scene = parseGeneratedScene(result.text, episode.episodeNo)
```

### 3. Fallback机制弱

**只有模型级fallback，无内容级fallback**：

```typescript
// run-script-generation-batch.ts:63
allowFallback: true // 只控制模型切换
```

### 4. 阻塞逻辑问题

**`build-episode-plans.ts:43`** — 一个验证失败阻塞整批：

```typescript
blocked: !input.stageValidation.ready // 单一验证失败阻塞所有集
```

---

## 八、AI推理质量问题

### 1. 上下文溢出风险

**`runtime-policy.ts:32`** — 硬限制80000 tokens但未实时检测：

```typescript
const HARD_CONTEXT_LIMIT = 80000
// 没有在路由前检查实际prompt长度
```

### 2. 模型路由逻辑缺陷

**`runtime-policy.ts:77`** — 硬编码只允许deepseek：

```typescript
const activeLanes = enabledLanes.filter(
  (lane): lane is ModelRouteLane => lane === 'deepseek' // ← 硬编码！其他lane被忽略
)
```

### 3. 系统指令污染

**`ai-provider-invoke.ts:29-36`** — 指令优先级混乱：

```typescript
const systemInstruction =
  resolveLaneSystemInstruction(lane, runtimeConfig) || request.systemInstruction // request优先级不清
```

### 4. Token估算问题

**`execute-script-repair.ts:31`** — 硬编码16000：

```typescript
estimatedContextTokens: 16000 // 硬编码，没有实际计算
```

---

## 九、数据一致性问题

### 1. Store写入无原子性保障

所有save函数都调用同一个updateProject，如果中途失败，数据可能部分写入。

### 2. JSON.parse失败回退到DEFAULT_STORE

**`project-store-core.ts:60`**：

```typescript
} catch {
  raw = ''  // 任何解析错误都回退，可能丢失数据
}
```

### 3. 规范化函数静默替换无效值

**`useStageStore.ts:46-54`** — EpisodeNo处理：

```typescript
episodeNo: Number.isFinite(Number(item?.episodeNo)) && Number(item.episodeNo) > 0
  ? Math.floor(Number(item.episodeNo))
  : index + 1 // 无效值被静默替换为index+1
```

---

## 十、剧情质量系统问题

### 1. 审计问题类型（20+种）

**`audit-scene-issues.ts`** 定义了详细的审计问题：

| 问题类型                    | 严重度 | 说明               |
| --------------------------- | ------ | ------------------ |
| `script_empty`              | high   | 剧本为空           |
| `screenplay_format_invalid` | high   | 非正式剧本格式     |
| `episode_heading_missing`   | high   | 缺少"第X集"标题    |
| `scene_heading_invalid`     | high   | 场次标题不合规     |
| `character_roster_missing`  | high   | 缺少"人物："行     |
| `action_beat_missing`       | high   | 缺少"△动作"提示    |
| `legacy_marker_detected`    | high   | 残留旧格式标记     |
| `section_polluted`          | high   | 三段内容串段       |
| `report_style_detected`     | high   | 带分析报告口吻     |
| `duplicated`                | high   | 场次实质重复       |
| `f6_expository_dialogue`    | medium | 对白直接说明事情   |
| `f6_character_voice_weak`   | medium | 人物说话味道不清   |
| `f6_stilted_dialogue`       | medium | 对白说出口感发卡   |
| `f6_playability_weak`       | medium | 动作对白情绪未扣成 |
| `progression_chain_weak`    | medium | 推进链维度不足     |

### 2. 修复系统只处理前5个问题

**`build-script-repair-plan.ts:32`**：

```typescript
suggestions: report.issues.slice(0, 5).map((issue) => {
```

如果审计发现20个问题，只修复前5个。

### 3. 修复失败静默吞错

**`execute-script-repair.ts:76`**：

```typescript
} catch {
  // 静默失败，不通知用户
}
```

### 4. Trait Binding只检测不评估强度

**`audit-character-issues.ts:13`** — 只检查是否有signal，但不检查binding强度。

### 5. Memory Echo回归检测只有两种状态

**`ledger-postflight-momentum-updates.ts:29-43`** — 只有"丢失"和"回归"，没有"弱化"状态。

---

## 问题严重度分级

| 级别    | 问题                     | 数量  |
| ------- | ------------------------ | ----- |
| 🔴 严重 | 循环依赖                 | 1     |
| 🔴 严重 | Promise链静默吞错误      | 1     |
| 🔴 严重 | 第二模型 fast lane默认值BUG    | 1     |
| 🔴 严重 | 重复函数定义             | 6组   |
| 🔴 严重 | 多入口修改同一状态       | 5组   |
| 🔴 严重 | 状态机单集失败级联整批   | 1     |
| 🔴 严重 | 解析失败无自救           | 1     |
| 🟠 中等 | 类型定义多处重复         | 3处   |
| 🟠 中等 | 进度时间三值不一         | 2处   |
| 🟠 中等 | Fire-and-forget void模式 | 8处   |
| 🟠 中等 | Timeout值分散            | 4+处  |
| 🟠 中等 | Retry逻辑分散            | 3处   |
| 🟠 中等 | 模型路由硬编码           | 1     |
| 🟠 中等 | 修复只处理前5个问题      | 1     |
| 🟡 轻微 | 调试代码残留             | 8处   |
| 🟡 轻微 | @ts-ignore绕过类型检查   | 2处   |
| 🟡 轻微 | 归档目录死代码           | 5文件 |

---

## 影响稳定产出优秀作品的关键问题

### TOP 5（按影响产出质量排序）

1. **解析失败无自救** — `run-script-generation-batch.ts:70` AI输出格式偏差导致整批失败
2. **状态机单集失败级联整批** — `state-machine.ts:46` 一集失败毁掉整批
3. **Promise链静默吞错误** — `project-store-core.ts:16-20` 数据可能悄悄丢失
4. **模型路由硬编码deepseek** — `runtime-policy.ts:77` 无法使用其他模型
5. ~~**进度时间三值差4倍** — 70/90/300秒，进度条ETA严重误导~~（B 侧前端展示口径已收口，现降级为历史问题）

### TOP 10（按严重度）

1. 循环依赖 — `structured-chain.ts ↔ structured-inference.ts`
2. Promise链静默吞错误 — `storeOpChain.then(()=>undefined, ()=>undefined)`
3. 第二模型 fast lane默认值BUG — `provider-config.ts:44` 用了DeepSeek默认值
4. 重复函数定义 — 6组相同逻辑分散多处
5. 多入口修改同一状态 — generationStatus被8处修改
6. 状态机单集失败级联整批 — `state-machine.ts:46`
7. 解析失败无自救 — `run-script-generation-batch.ts:70-72`
8. 模型路由硬编码deepseek — `runtime-policy.ts:77`
9. 修复只处理前5个问题 — `build-script-repair-plan.ts:32`
10. ~~进度时间三值不一 — `DETAILED_OUTLINE_ESTIMATED_SECONDS: 70/90/300`~~（展示层已统一，保留作历史审计记录）

